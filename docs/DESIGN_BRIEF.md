# DESIGN_BRIEF — ТулаКлимат

> Эталон визуала — прототипы в `design/` (локально, **в репозиторий не выкладываются**). Здесь извлечены токены и правила, по которым дизайн переносится в код: этого документа достаточно для работы без доступа к исходникам.
>
> Правила стилизации: [CLAUDE.md](CLAUDE.md)

Все значения ниже **сняты с прототипа**, а не придуманы. Если в макете `border-radius: 11px` — в коде тоже `11px`.

---

## 1. Характер

Технологичный минимализм с холодной палитрой: бирюза на белом, много воздуха, тонкие линии вместо теней, крупные заголовки в гротеске и моноширинный шрифт для цифр.

Тон — «мы монтажники, которые объясняют, как всё устроено на самом деле». Отсюда таблицы, цифры, разборы обмана и никакого стокового глянца.

---

## 2. Цветовые токены

Определяются в `shared/styles/tokens.css` на `:root`, переопределяются на `html[data-theme="dark"]`. 🔴 В CSS-модулях — только переменные, ни одного хардкоженного цвета.

| Переменная                  | Светлая                                | Тёмная                                    | Назначение                 |
| --------------------------- | -------------------------------------- | ----------------------------------------- | -------------------------- |
| `--bg`                      | `#FFFFFF`                              | `#0B1220`                                 | фон страницы               |
| `--bg-soft`                 | `#F8FAFC`                              | `#0E1729`                                 | фон чередующихся секций    |
| `--card`                    | `#FFFFFF`                              | `#121D31`                                 | карточки                   |
| `--header-bg`               | `rgba(255,255,255,.82)`                | `rgba(11,18,32,.8)`                       | шапка со стеклом           |
| `--hero-grad`               | `linear-gradient(180deg,#F0F9FB,#FFF)` | `linear-gradient(180deg,#0C1628,#0B1220)` | фон первого экрана         |
| `--ink`                     | `#0F172A`                              | `#EAF1F8`                                 | заголовки                  |
| `--ink2`                    | `#334155`                              | `#C9D5E2`                                 | акцентный текст, навигация |
| `--body`                    | `#475569`                              | `#A8B6C6`                                 | основной текст             |
| `--muted`                   | `#556173`                              | `#8598AC`                                 | подписи                    |
| `--faint`                   | `#5B6A7D`                              | `#7E92A7`                                 | прочерки, плейсхолдеры     |
| `--line`                    | `#E2E8F0`                              | `#1E2B41`                                 | границы                    |
| `--line-strong`             | `#CBD5E1`                              | `#2C3D59`                                 | границы полей и кнопок     |
| `--line-soft`               | `#EEF2F6`                              | `#182338`                                 | разделители строк таблиц   |
| `--accent-ink`              | `#0891B2`                              | `#22D3EE`                                 | границы и обводки акцента  |
| `--accent-text`             | `#0E7490`                              | `#22D3EE`                                 | акцентный текст и иконки   |
| `--accent-bg`               | `#ECFEFF`                              | `rgba(34,211,238,.10)`                    | подложка акцентных плашек  |
| `--accent-line`             | `#A5F3FC`                              | `rgba(34,211,238,.35)`                    | границы акцентных плашек   |
| `--panel`                   | `#0F172A`                              | `#111D34`                                 | тёмные панели-врезки       |
| `--stripe-a` / `--stripe-b` | `#EDF2F6` / `#F5F8FA`                  | `#16223A` / `#121D31`                     | зебра таблиц               |
| `--field`                   | `#FFFFFF`                              | `#0B1424`                                 | фон полей ввода            |

🔴 **Грунт `[data-ground='panel']`** (ADR-158). Подвал, карточка на `--panel` и
блок напоминания — тёмные острова посреди светлой страницы, и уровни,
подобранные под белый фон, на них не читаются. Внутри грунта `--ink`, `--ink2`,
`--body`, `--muted`, `--faint` и `--accent-text` берут значения тёмной темы.
Компонент про грунт не знает: он ставит атрибут, цвета остаются переменными.

**Фирменные константы**, одинаковые в обеих темах: `#0E7490` — основная кнопка; `#155E75` — её ховер; градиент акцента `#0E7490 → #06B6D4 → #22D3EE → #0891B2`; выделение текста `::selection` — `#22D3EE` на `#0F172A`.

Тема определяется до первого кадра инлайн-скриптом (`localStorage.tk-theme`, иначе `prefers-color-scheme`) — иначе будет вспышка светлой темы у пользователя с тёмной. Скрипт остаётся синхронным в `<head>`.

---

## 3. Типографика

🔴 **Гарнитуры прототипа заменены.** Space Grotesk и Space Mono **не содержат кириллицы** — в макете все русские заголовки рисовались системным шрифтом, а не тем, что стоит в CSS. Подробности и обоснование — ADR-025.

| Шрифт              | Где                                          | Начертания                  |
| ------------------ | -------------------------------------------- | --------------------------- |
| **Onest**          | заголовки, кнопки, цены, всё «инженерное»    | 600 / 700 / 800             |
| **Manrope**        | основной текст, интерфейс                    | 400 / 500 / 600 / 700 / 800 |
| **JetBrains Mono** | таймлайн монтажа, технические подписи, метки | 400                         |

Все три под SIL Open Font License 1.1 — свободное коммерческое использование.

🔴 Самохостинг: файлы в `public/fonts`, `@font-face` описаны вручную в `fonts.css` с сохранёнными `unicode-range`. Ни `next/font/google`, ни запросов к Google в рантайме и на сборке. Кириллица грузится отдельным файлом и только когда нужна.

**Шкала прототипа (десктоп):**

| Роль                | Размер             | Начертание                          | Трекинг |
| ------------------- | ------------------ | ----------------------------------- | ------- |
| H1                  | 49px / 1.06        | Space Grotesk 700                   | −0.03em |
| H2 секции           | 38px               | Space Grotesk 700                   | −0.02em |
| H3 карточки         | 17→19px, текучий   | Space Grotesk 700                   | −0.02em |
| Лид под H1          | 16→18.5px, текучий | Manrope 400                         | —       |
| Текст               | 15px / 1.55        | Manrope 400                         | —       |
| Текст плотный       | 14.5px             | Manrope 400                         | —       |
| Подписи             | 13px               | Manrope 500                         | —       |
| Микроподписи        | 10.5–12.5px        | Manrope 600                         | 0.05em  |
| Надзаголовки секций | 12px               | Space Mono / Manrope 700, uppercase | 0.14em  |
| Цифры и цены        | Space Grotesk 700  | —                                   | —       |

Дробные размеры (14.5, 16.5, 17.5) — не случайность инструмента, а часть ритма. Переносим как есть.

🔴 **Кегли H1–H3 и лид — непрерывные функции ширины** (§6): числа в таблице
называют потолок на **1200** — эталонной ширине прототипа — и пол на 320, а
между ними размер меняется на каждом пикселе. Прежние `clamp(30px, 8.5vw, 49px)`
упирались в потолок уже на 576, то есть от планшета до широкого десктопа кегль
не менялся вовсе.

Нижние границы (17px у H3, 16px у лида) сняты не с прототипа — его на 320 не
рисовали. Они выбраны так, чтобы на нижней границе поддержки текст оставался
читаемым, и это единственные числа брифа, у которых источник — решение, а не
макет.

---

## 4. Геометрия

**Радиусы:** 9–11px — мелкие элементы (кнопки-иконки, чипы, поля); 12–14px — кнопки и карточки; 16–20px — крупные карточки и панели; 22–24px — секционные блоки; 100px — пилюли и чипы фильтров.

Доминирует **11px** — это радиус по умолчанию.

**Сетка:** контейнер `max-width: 1200px`, боковые отступы текучие — 16px на 320, 24px на 1200 (§6). Вертикальный ритм секции — `88px 24px`. Текстовые блоки ограничены 620–680px по ширине строки.

**Тени** — мягкие, с отрицательным разбросом, всегда цветные (не серые):
`0 12px 34px -16px rgba(15,23,42,.22)` — шапка · `0 20px 44px -28px rgba(15,23,42,.2)` — карточки · `0 10px 24px rgba(14,116,144,.3)` — основная кнопка · `0 0 0 3px rgba(8,145,178,.12)` — фокус.

Фокус-кольцо `0 0 0 3px rgba(8,145,178,.12)` — обязательный видимый фокус для клавиатуры, не удалять.

---

## 5. Инвентарь компонентов

**`shared/ui`:** Button (primary / secondary / ghost, 3 размера) · IconButton · Chip (выбор, с активным состоянием) · Input · Textarea · Select · Checkbox · RangeSlider · FileInput · Card · Badge · Table · Accordion · Modal · Skeleton · Rating (звёзды, ввод и вывод) · ThemeToggle.

**`widgets`:** Header (стеклянная шапка, sticky) · Hero с подбором · TrustStrip · ServicesGrid · CatalogGrid · CompareTable · SavingsBlock · StepsTimeline · PriceTable · Calculator · HonestPricing · ScamAccordion · DiagnosticsPicker · WhyUs · Reviews + ReviewForm · LeadForm · KnowledgeTeaser · Faq · Contacts · Footer.

Экраны собираются из уже готовых компонентов UI Kit, а не наоборот.

---

## 6. 🔴 Адаптив — проектируем сами

**В прототипе ноль медиа-запросов.** Всё на фиксированных пикселях: `font-size: 49px` у H1, гриды вида `1.2fr 1.1fr 0.6fr auto`, таблица сравнения в четыре колонки. На телефоне это развалится.

В нише «кондиционеры + город» с телефона приходит большая часть трафика, и мобильная вёрстка входит в оценку Яндекса напрямую. Поэтому адаптив — не доводка в конце, а часть каждой секции.

### 🔴 Порогов ровно три

| Диапазон | Имя     | Что это                                        |
| -------- | ------- | ---------------------------------------------- |
| 320–599  | телефон | нижняя граница поддержки — **320px**           |
| 600–899  | крупный | телефон боком, узкий планшет                   |
| 900–1199 | планшет |                                                |
| ≥ 1200   | десктоп | контейнер упирается в 1200, дальше растут поля |

**Пороги: 600, 900, 1200.** Других в коде нет и быть не должно: до этой
работы их было семнадцать на 113 медиа-запросов, и раскладка ломалась в
случайных местах — на 359, 399, 480, 520, 560, 620, 700, 720, 760, 1040, 1100. Проверяется машиной: `scripts/css-breakpoints.test.mjs` роняет прогон на
любом пороге вне набора.

**Внутри диапазона макет тянется, а не стоит.** Кегли, боковые поля,
вертикальный ритм секций и внутренние отступы карточек — непрерывные функции
ширины через `clamp()`. Скачок раскладки допустим только на пороге.

#### Исключения — список закрытый

| Порог | Где                      | Почему                                                                                 |
| ----- | ------------------------ | -------------------------------------------------------------------------------------- |
| 1000  | полная навигация в шапке | семь пунктов, бренд, телефон и кнопка заявки раньше не помещаются — считано по ширинам |

Компонент вправе объявить свой порог, **только** если так диктует его
содержимое, и тогда порог попадает в эту таблицу с причиной. Список без
причины — не список исключений, а второй набор порогов.

**Правила деградации:**

| Элемент                   | На мобильном                                                                                                                   |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| H1 49px                   | `clamp(30px, 2.159vw + 23.091px, 49px)` — 30 на 320, 49 на 1200                                                                |
| H2 38px                   | `clamp(24px, 1.591vw + 18.909px, 38px)`                                                                                        |
| Секция `88px 24px`        | `clamp(48px, 4.55vw + 33.45px, 88px)` по вертикали, `clamp(16px, 0.91vw + 13.09px, 24px)` по горизонтали                       |
| Гриды `1fr 1fr` и сложнее | одна колонка                                                                                                                   |
| Таблица сравнения         | горизонтальный скролл внутри своего контейнера с залипающей первой колонкой; страница по горизонтали не скроллится **никогда** |
| Таблица цен               | карточки вместо строк                                                                                                          |
| Навигация в шапке         | бургер-меню; ниже 600 в полосе только бренд и кнопка меню — телефон и заявка уезжают в липкую панель действий (ADR-174)        |
| Калькулятор               | поля в столбец, итог залипает снизу экрана                                                                                     |
| 3D-наклон карточек        | отключён (нет указателя)                                                                                                       |
| Частицы на canvas         | отключены на `<900px` и при `prefers-reduced-motion`                                                                           |

Тап-зоны — минимум 44×44px. Телефон везде, где он показан, — ссылка `tel:`:
в шапке с 600px, в подвале страницы, в подвале выдвижного меню и в липкой
панели действий на телефоне.

**Высота шапки — 56px до 600 и 60px с 600.** Это единственный скачок раскладки
в шапке, и оправдан он появлением телефона и кнопки заявки; всё остальное между
порогами тянется. Пункт выдвижного меню — 52px при минимуме 44.

### Липкая панель действий — ниже 600

Главная на 375 — почти двадцать семь экранов прокрутки, и оба целевых действия
живут только в шапке. Ниже 600 у нижнего края появляется панель: «Позвонить» и
«Оставить заявку» там, куда дотягивается большой палец.

| Значение          | Что                                                                                            |
| ----------------- | ---------------------------------------------------------------------------------------------- |
| Высота            | 69px = 10 сверху + тап-зона 44 + 14 снизу + граница 1px (`--action-bar-h`)                     |
| Поля              | `10px var(--gutter) calc(14px + env(safe-area-inset-bottom))`                                  |
| Фон и граница     | `--header-bg` с `backdrop-filter: blur(16px)`, сверху `--line`                                 |
| Зазор кнопок      | 10px, горизонтальные поля кнопок 12px вместо кнопочных 18                                      |
| Доли ширины       | звонок `1`, заявка `1.4`; на 320 звонок упирается в свою подпись и берёт 127 из 288            |
| Счётчик сравнения | `--font-mono` 12px, пилюля `color-mix(in srgb, var(--panel) 30%, transparent)`                 |
| Появление / уход  | первый экран уехал вверх / форма заявки в кадре — `IntersectionObserver`, без слушателя scroll |

🔴 **Пилюля счётчика темнее кнопки, а не светлее.** Белая подложка 22% из
макета даёт с белой цифрой 3,53:1 и не проходит AA ни в одной теме — замерено в
браузере. Затемнение той же прозрачности даёт 7,9:1 и 7,68:1.

🔴 **С 600 панели нет.** Телефон и кнопка заявки видны в шапке, и два места для
одного действия соревнуются между собой: побеждает то, что ближе к пальцу, а
проигрывают оба. Ниже 600 по той же причине нет плавающей кнопки «наверх» — её
место занято, а возврат наверх на телефоне делают жестом по строке состояния.

🔴 **Странице нужен нижний резерв** — `--bottom-reserve`, отступ снизу у подвала:
высота панели плюс `env(safe-area-inset-bottom)` на телефоне, высота кнопки
«наверх» плюс её отступ на остальных ширинах. Без него липкий элемент накрывает
последнюю строку подвала — ссылку на политику обработки персональных данных,
которая обязана оставаться доступной.

---

## 7. Движение

Из прототипа переносим: появление секций при скролле (opacity + `translateY(28px)`, `cubic-bezier(.2,.7,.2,1)`, .75s) · счётчики цифр (1.2s, ease-out cubic) · анимированный градиент в H1 (6s) · пульсирующее кольцо у индикатора «на связи».

Все декоративные эффекты: не блокируют первый экран · выключаются при `prefers-reduced-motion: reduce` · не участвуют в LCP.

Эффект, стоящий бюджета производительности, убирается. Бюджет не двигается.

---

## 8. Изображения

Через `next/image`, с явными размерами. `alt` осмысленный и с географией: «Сплит-система 09 — купить в Туле с установкой».

У модели без фото — аккуратная заглушка с классом мощности, а не битая картинка и не пустое место (в сидах у всех моделей `photo: ''`, так что заглушка — рабочее состояние, а не крайний случай).

Приоритетную загрузку (`priority`) получает только изображение первого экрана.

---

## 9. Админка

Другой визуальный язык: плотнее, строже, без анимаций и без тёмных «продающих» панелей. Боковая навигация, разделы **Компания** / Реквизиты / Цены / **Каталог** / База знаний / Отзывы / **Заявки**.

Те же токены и те же компоненты UI Kit — админка не заводит собственную дизайн-систему.

Три раздела проектируются нами, в исходном дизайне их нет:

- **Компания** — все данные организации сгруппированы по смыслу (контакты, адрес и координаты, часы, регион, реквизиты, гарантия и оплата, соцсети, метаданные главной). Незаполненные поля подсвечиваются: индикатор готовности сверху показывает, что осталось до запуска, чтобы заглушка не уехала в прод.
- **Заявки** — список с фильтром по статусу, карточка со всеми полями, фото места установки, смена статуса, комментарий менеджера.
- **Каталог** расширяется: несколько фото с выбором главного перетаскиванием, поля скидки, произвольные характеристики строками, порядок вывода.

## 10. Скидка на витрине

Цена со скидкой — единственное место, где допустим красный акцент; в остальном палитра холодная.

| Элемент          | Как рисуется                                                                             |
| ---------------- | ---------------------------------------------------------------------------------------- |
| Действующая цена | Space Grotesk 700, обычный размер цены, цвет `--ink`                                     |
| Старая цена      | рядом, на 2–3px мельче, `--faint`, зачёркнута                                            |
| Плашка процента  | пилюля `border-radius: 100px`, компактная, с вычисленным «−12%» либо текстом `saleLabel` |
| Срок действия    | микроподпись `--muted`: «до 31 октября». Показывается, только если `saleTo` задан        |

Плашка не перекрывает фото и не превращается в «кричащий» стикер: сайт продаёт честностью сметы, и агрессивная распродажная эстетика противоречит остальным экранам.

Товар без скидки рисуется ровно как раньше — никаких пустых мест и «−0%». Состояние «скидки нет» основное, «скидка есть» — дополнение к нему.

---

## 11. Логотип и фавиконка

Утверждён вариант **1a «Поток»** из раунда логотипов (`design/Логотипы.dc.html`). Знак — скруглённый квадрат с тремя струями воздуха: прямой образ работы кондиционера, читается в 16px.

🔴 **Логотип в шапке прототипа главной страницы устарел** — там иконка внутреннего блока на `#0E7490`. Она отменена, используется 1a.

Исходники лежат в репозитории: [`assets/brand/`](../assets/brand).

| Файл            | Что это                                                                  | Где применяется            |
| --------------- | ------------------------------------------------------------------------ | -------------------------- |
| `mark.svg`      | знак на светлом: плитка `#0891B2`, две струи `#FFFFFF`, третья `#A5F3FC` | шапка, футер, письма       |
| `mark-dark.svg` | знак на тёмном: плитка `#22D3EE`, струи `#0F172A`                        | тёмная тема, тёмные панели |
| `icon.svg`      | фавиконка                                                                | вкладка браузера, закладки |

**Геометрия знака.** `viewBox="0 0 48 48"`, скругление `rx="13"`, три кривые с `stroke-linecap="round"`. Пропорции менять нельзя — знак строился под масштабирование до 16px.

🔴 **Толщина штриха зависит от размера** — это оптическая компенсация из макета, а не небрежность:

| Размер        | `stroke-width` |
| ------------- | -------------- |
| 40px и больше | 3.4            |
| 24–39px       | 3.8            |
| 16–23px       | 4.4            |

При 3.4 в шестнадцати пикселях струи сливаются с плиткой. Поэтому фавиконка — отдельный файл с утолщённым штрихом, а не тот же `mark.svg`, отмасштабированный вниз.

**Горизонтальный замок:** знак + «ТулаКлимат» (Space Grotesk 700, `-0.02em`) + подпись «кондиционеры в Туле» (`--fs-micro` 12px, `letter-spacing: 0.06em`, `--muted`), межколоночный интервал 12px, выравнивание по вертикальному центру. На узких экранах подпись убирается, остаётся знак с названием.

**Название бренда в замке приходит из настроек компании** (`company.name`), а не зашито в компонент: «ТулаКлимат» — рабочее имя из макета, не утверждённый бренд.

**В Next 15** знак подключается как `src/app/icon.svg` (фавиконка генерируется фреймворком), плюс `apple-icon` и `opengraph-image` на его основе. В компонентах — инлайн-SVG, чтобы знак наследовал тему и не давал лишнего запроса.

---

## 12. Два файла токенов

`tokens.css` — то, что **снято с макета**: палитра обеих тем, шкала, радиусы, тени. Менять эти значения можно только вслед за дизайном.

`ui-tokens.css` — то, что **выведено нами**, потому что в макете этого не было: цвета ошибки, скидки, звёзд рейтинга, подложки модального окна, длительности переходов, минимальная тап-зона. Значения записаны в `rgb()`, а не хексом, — правило stylelint про хекс действует и здесь.

Разделение сохраняет происхождение: через полгода видно, что пришло от дизайнера, а что мы додумали сами. Оба файла подключаются из `global.css`, компонентам разницы нет.

### Отступления от макета ради контраста

Прототип местами не проходит AA, а доступность у нас обязательна ([CLAUDE.md](CLAUDE.md)). Изменено:

| Было в макете                   | Стало                     | Почему                                                           |
| ------------------------------- | ------------------------- | ---------------------------------------------------------------- |
| `#EF4444` (ошибка)              | `rgb(190 18 60)`          | на белом не добирал 4.5:1                                        |
| `#F59E0B` (предупреждение)      | `rgb(217 119 6)`          | то же                                                            |
| `#0891B2` — фон выбранного чипа | `--brand` `#0E7490`       | белый текст на бирюзе даёт 3.5:1                                 |
| `#94A3B8` — `--faint`           | `#5B6A7D`                 | 2.6:1 на белом; 535 мест мимо AA (ADR-158)                       |
| `#64748B` — `--muted`           | `#556173`                 | сдвинут следом, чтобы ступень между уровнями осталась            |
| `#0891B2` — акцентный текст     | `--accent-text` `#0E7490` | 3.7:1 на белом; границам прежний токен оставлен, им довольно 3:1 |
| 10–11.5px — служебные подписи   | `--fs-micro` 12px         | складывалось с контрастом 2.6:1 — на телефоне не читалось вовсе  |

Это осознанный приоритет стандарта над макетом, а не вкусовщина. Если дизайнер обновит палитру — сверяться нужно с этой таблицей.

🔴 Пары «цвет × фон» проверяет `shared/styles/contrast.test.ts`: 24 случая в трёх
палитрах. Меняя значение в макете или в коде, прогоняйте её — на глаз каждое
отдельное место выглядит приемлемо, и дефект копится незаметно.

---

## 13. Что уже есть — проверь, прежде чем верстать

🔴 Список для агентов блоков. Правило слоёв запрещает импорт вбок между виджетами, поэтому любая общая деталь, не найденная здесь, размножается копиями по блокам. Так уже вышло с утилитой для скринридеров: шесть одинаковых определений в шести файлах.

**Общие стили** (`shared/styles/global.css`) — подключены глобально, класс доступен из любого модуля:

| Класс     | Что делает                                                            |
| --------- | --------------------------------------------------------------------- |
| `.srOnly` | текст только для скринридеров: подписи, нужные голосом, но не глазами |

**Токены** — `tokens.css` (снято с макета) и `ui-tokens.css` (выведено нами: ошибка, скидка, звёзды, оверлей, длительности, `--tap`). Полный список — §2 и §12.

**Доменные функции** — не переписывай, они покрыты тестами:

| Функция                                                 | Откуда                                       |
| ------------------------------------------------------- | -------------------------------------------- |
| `calculateInstallation`                                 | `@/entities/price/lib/calculateInstallation` |
| `getActivePrice`                                        | `@/entities/product/lib/getActivePrice`      |
| `pickByArea`, `buildCompareTable`                       | `@/entities/product/lib/*`                   |
| `parseArticleBody`                                      | `@/entities/article/lib/*`                   |
| `formatMoney`, `formatPhone`, `formatDate`, `phoneHref` | `@/shared/lib/format`                        |
| `slugify`, `uniqueSlug`, `pageSlug`                     | `@/shared/lib/slug`                          |
| `useCountUp`                                            | `@/shared/lib/useCountUp`                    |

**UI Kit** — 20 компонентов, инвентарь в §5, живой список историй на http://localhost:6006.

Если нужного нет — это **блокер**: сделай минимум локально и напиши об этом в отчёте. Не заводи переиспользуемый примитив у себя в блоке молча.
