# DESIGN_BRIEF — ТулаКлимат

> Эталон визуала — прототипы в `design/` (локально, **в репозиторий не выкладываются**). Здесь извлечены токены и правила, по которым дизайн переносится в код: этого документа достаточно для работы без доступа к исходникам.
>
> Правила стилизации: [CLAUDE.md](CLAUDE.md)

Все значения ниже **сняты с прототипа**, а не придуманы. Если в макете `border-radius: 11px` — в коде тоже `11px`.

---

## 1. Характер

Технологичный минимализм с холодной палитрой: бирюза на белом, много воздуха, тонкие линии вместо теней, крупные заголовки в гротеске и моноширинный шрифт для цифр.

Тон — «мы монтажники, которые объясняют, как всё устроено на самом деле». Отсюда таблицы, цифры, разборы обмана и никакого стокового глянца.

---

## 2. Цветовые токены

Определяются в `shared/styles/tokens.css` на `:root`, переопределяются на `html[data-theme="dark"]`. 🔴 В CSS-модулях — только переменные, ни одного хардкоженного цвета.

| Переменная | Светлая | Тёмная | Назначение |
|---|---|---|---|
| `--bg` | `#FFFFFF` | `#0B1220` | фон страницы |
| `--bg-soft` | `#F8FAFC` | `#0E1729` | фон чередующихся секций |
| `--card` | `#FFFFFF` | `#121D31` | карточки |
| `--header-bg` | `rgba(255,255,255,.82)` | `rgba(11,18,32,.8)` | шапка со стеклом |
| `--hero-grad` | `linear-gradient(180deg,#F0F9FB,#FFF)` | `linear-gradient(180deg,#0C1628,#0B1220)` | фон первого экрана |
| `--ink` | `#0F172A` | `#EAF1F8` | заголовки |
| `--ink2` | `#334155` | `#C9D5E2` | акцентный текст, навигация |
| `--body` | `#475569` | `#A8B6C6` | основной текст |
| `--muted` | `#64748B` | `#8598AC` | подписи |
| `--faint` | `#94A3B8` | `#7E92A7` | прочерки, плейсхолдеры |
| `--line` | `#E2E8F0` | `#1E2B41` | границы |
| `--line-strong` | `#CBD5E1` | `#2C3D59` | границы полей и кнопок |
| `--line-soft` | `#EEF2F6` | `#182338` | разделители строк таблиц |
| `--accent-ink` | `#0891B2` | `#22D3EE` | акцентный текст и иконки |
| `--accent-bg` | `#ECFEFF` | `rgba(34,211,238,.10)` | подложка акцентных плашек |
| `--accent-line` | `#A5F3FC` | `rgba(34,211,238,.35)` | границы акцентных плашек |
| `--panel` | `#0F172A` | `#111D34` | тёмные панели-врезки |
| `--stripe-a` / `--stripe-b` | `#EDF2F6` / `#F5F8FA` | `#16223A` / `#121D31` | зебра таблиц |
| `--field` | `#FFFFFF` | `#0B1424` | фон полей ввода |

**Фирменные константы**, одинаковые в обеих темах: `#0E7490` — основная кнопка; `#155E75` — её ховер; градиент акцента `#0E7490 → #06B6D4 → #22D3EE → #0891B2`; выделение текста `::selection` — `#22D3EE` на `#0F172A`.

Тема определяется до первого кадра инлайн-скриптом (`localStorage.tk-theme`, иначе `prefers-color-scheme`) — иначе будет вспышка светлой темы у пользователя с тёмной. Скрипт остаётся синхронным в `<head>`.

---

## 3. Типографика

| Шрифт | Где | Начертания |
|---|---|---|
| **Space Grotesk** | заголовки, кнопки, цены, всё «инженерное» | 400 / 500 / 600 / 700 |
| **Manrope** | основной текст, интерфейс | 400 / 500 / 600 / 700 / 800 |
| **Space Mono** | таймлайн монтажа, технические подписи, метки | 400 |

🔴 Self-host через `next/font/local`, подмножество кириллицы, `display: swap`. Никаких запросов к Google Fonts в рантайме.

**Шкала прототипа (десктоп):**

| Роль | Размер | Начертание | Трекинг |
|---|---|---|---|
| H1 | 49px / 1.06 | Space Grotesk 700 | −0.03em |
| H2 секции | 38px | Space Grotesk 700 | −0.02em |
| H3 карточки | 19–22px | Space Grotesk 700 | −0.02em |
| Лид под H1 | 17.5px | Manrope 400 | — |
| Текст | 15px / 1.55 | Manrope 400 | — |
| Текст плотный | 14.5px | Manrope 400 | — |
| Подписи | 13px | Manrope 500 | — |
| Микроподписи | 10.5–12.5px | Manrope 600 | 0.05em |
| Надзаголовки секций | 12px | Space Mono / Manrope 700, uppercase | 0.14em |
| Цифры и цены | Space Grotesk 700 | — | — |

Дробные размеры (14.5, 16.5, 17.5) — не случайность инструмента, а часть ритма. Переносим как есть.

---

## 4. Геометрия

**Радиусы:** 9–11px — мелкие элементы (кнопки-иконки, чипы, поля); 12–14px — кнопки и карточки; 16–20px — крупные карточки и панели; 22–24px — секционные блоки; 100px — пилюли и чипы фильтров.

Доминирует **11px** — это радиус по умолчанию.

**Сетка:** контейнер `max-width: 1200px`, боковые отступы 24px. Вертикальный ритм секции — `88px 24px`. Текстовые блоки ограничены 620–680px по ширине строки.

**Тени** — мягкие, с отрицательным разбросом, всегда цветные (не серые):
`0 12px 34px -16px rgba(15,23,42,.22)` — шапка · `0 20px 44px -28px rgba(15,23,42,.2)` — карточки · `0 10px 24px rgba(14,116,144,.3)` — основная кнопка · `0 0 0 3px rgba(8,145,178,.12)` — фокус.

Фокус-кольцо `0 0 0 3px rgba(8,145,178,.12)` — обязательный видимый фокус для клавиатуры, не удалять.

---

## 5. Инвентарь компонентов

**`shared/ui`:** Button (primary / secondary / ghost, 3 размера) · IconButton · Chip (выбор, с активным состоянием) · Input · Textarea · Select · Checkbox · RangeSlider · FileInput · Card · Badge · Table · Accordion · Modal · Skeleton · Rating (звёзды, ввод и вывод) · ThemeToggle.

**`widgets`:** Header (стеклянная шапка, sticky) · Hero с подбором · TrustStrip · ServicesGrid · CatalogGrid · CompareTable · SavingsBlock · StepsTimeline · PriceTable · Calculator · HonestPricing · ScamAccordion · DiagnosticsPicker · WhyUs · Reviews + ReviewForm · LeadForm · KnowledgeTeaser · Faq · Contacts · Footer.

Экраны собираются из уже готовых компонентов UI Kit, а не наоборот.

---

## 6. 🔴 Адаптив — проектируем сами

**В прототипе ноль медиа-запросов.** Всё на фиксированных пикселях: `font-size: 49px` у H1, гриды вида `1.2fr 1.1fr 0.6fr auto`, таблица сравнения в четыре колонки. На телефоне это развалится.

В нише «кондиционеры + город» с телефона приходит большая часть трафика, и мобильная вёрстка входит в оценку Яндекса напрямую. Поэтому адаптив — не доводка в конце, а часть каждой секции.

**Брейкпоинты:** `≥1200` десктоп (эталон прототипа) · `900–1199` планшет · `600–899` крупный телефон · `<600` телефон. Нижняя граница поддержки — **320px**.

**Правила деградации:**

| Элемент | На мобильном |
|---|---|
| H1 49px | `clamp(30px, 8.5vw, 49px)` |
| H2 38px | `clamp(24px, 6vw, 38px)` |
| Секция `88px 24px` | `clamp(48px, 9vw, 88px)` по вертикали, 16px по горизонтали |
| Гриды `1fr 1fr` и сложнее | одна колонка |
| Таблица сравнения | горизонтальный скролл внутри своего контейнера с залипающей первой колонкой; страница по горизонтали не скроллится **никогда** |
| Таблица цен | карточки вместо строк |
| Навигация в шапке | бургер-меню; телефон и кнопка заявки остаются видимыми |
| Калькулятор | поля в столбец, итог залипает снизу экрана |
| 3D-наклон карточек | отключён (нет указателя) |
| Частицы на canvas | отключены на `<900px` и при `prefers-reduced-motion` |

Тап-зоны — минимум 44×44px. Телефон в шапке и футере — ссылка `tel:`.

---

## 7. Движение

Из прототипа переносим: появление секций при скролле (opacity + `translateY(28px)`, `cubic-bezier(.2,.7,.2,1)`, .75s) · счётчики цифр (1.2s, ease-out cubic) · анимированный градиент в H1 (6s) · пульсирующее кольцо у индикатора «на связи».

Все декоративные эффекты: не блокируют первый экран · выключаются при `prefers-reduced-motion: reduce` · не участвуют в LCP.

Эффект, стоящий бюджета производительности, убирается. Бюджет не двигается.

---

## 8. Изображения

Через `next/image`, с явными размерами. `alt` осмысленный и с географией: «Сплит-система 09 — купить в Туле с установкой».

У модели без фото — аккуратная заглушка с классом мощности, а не битая картинка и не пустое место (в сидах у всех моделей `photo: ''`, так что заглушка — рабочее состояние, а не крайний случай).

Приоритетную загрузку (`priority`) получает только изображение первого экрана.

---

## 9. Админка

Другой визуальный язык: плотнее, строже, без анимаций и без тёмных «продающих» панелей. Боковая навигация, разделы **Компания** / Реквизиты / Цены / **Каталог** / База знаний / Отзывы / **Заявки**.

Те же токены и те же компоненты UI Kit — админка не заводит собственную дизайн-систему.

Три раздела проектируются нами, в исходном дизайне их нет:

- **Компания** — все данные организации сгруппированы по смыслу (контакты, адрес и координаты, часы, регион, реквизиты, гарантия и оплата, соцсети, метаданные главной). Незаполненные поля подсвечиваются: индикатор готовности сверху показывает, что осталось до запуска, чтобы заглушка не уехала в прод.
- **Заявки** — список с фильтром по статусу, карточка со всеми полями, фото места установки, смена статуса, комментарий менеджера.
- **Каталог** расширяется: несколько фото с выбором главного перетаскиванием, поля скидки, произвольные характеристики строками, порядок вывода.

## 10. Скидка на витрине

Цена со скидкой — единственное место, где допустим красный акцент; в остальном палитра холодная.

| Элемент | Как рисуется |
|---|---|
| Действующая цена | Space Grotesk 700, обычный размер цены, цвет `--ink` |
| Старая цена | рядом, на 2–3px мельче, `--faint`, зачёркнута |
| Плашка процента | пилюля `border-radius: 100px`, компактная, с вычисленным «−12%» либо текстом `saleLabel` |
| Срок действия | микроподпись `--muted`: «до 31 октября». Показывается, только если `saleTo` задан |

Плашка не перекрывает фото и не превращается в «кричащий» стикер: сайт продаёт честностью сметы, и агрессивная распродажная эстетика противоречит остальным экранам.

Товар без скидки рисуется ровно как раньше — никаких пустых мест и «−0%». Состояние «скидки нет» основное, «скидка есть» — дополнение к нему.

---

## 11. Логотип и фавиконка

Утверждён вариант **1a «Поток»** из раунда логотипов (`design/Логотипы.dc.html`). Знак — скруглённый квадрат с тремя струями воздуха: прямой образ работы кондиционера, читается в 16px.

🔴 **Логотип в шапке прототипа главной страницы устарел** — там иконка внутреннего блока на `#0E7490`. Она отменена, используется 1a.

Исходники лежат в репозитории: [`assets/brand/`](../assets/brand).

| Файл | Что это | Где применяется |
|---|---|---|
| `mark.svg` | знак на светлом: плитка `#0891B2`, две струи `#FFFFFF`, третья `#A5F3FC` | шапка, футер, письма |
| `mark-dark.svg` | знак на тёмном: плитка `#22D3EE`, струи `#0F172A` | тёмная тема, тёмные панели |
| `icon.svg` | фавиконка | вкладка браузера, закладки |

**Геометрия знака.** `viewBox="0 0 48 48"`, скругление `rx="13"`, три кривые с `stroke-linecap="round"`. Пропорции менять нельзя — знак строился под масштабирование до 16px.

🔴 **Толщина штриха зависит от размера** — это оптическая компенсация из макета, а не небрежность:

| Размер | `stroke-width` |
|---|---|
| 40px и больше | 3.4 |
| 24–39px | 3.8 |
| 16–23px | 4.4 |

При 3.4 в шестнадцати пикселях струи сливаются с плиткой. Поэтому фавиконка — отдельный файл с утолщённым штрихом, а не тот же `mark.svg`, отмасштабированный вниз.

**Горизонтальный замок:** знак + «ТулаКлимат» (Space Grotesk 700, `-0.02em`) + подпись «кондиционеры в Туле» (11px, `letter-spacing: 0.06em`, `--muted`), межколоночный интервал 12px, выравнивание по вертикальному центру. На узких экранах подпись убирается, остаётся знак с названием.

**Название бренда в замке приходит из настроек компании** (`company.name`), а не зашито в компонент: «ТулаКлимат» — рабочее имя из макета, не утверждённый бренд.

**В Next 15** знак подключается как `src/app/icon.svg` (фавиконка генерируется фреймворком), плюс `apple-icon` и `opengraph-image` на его основе. В компонентах — инлайн-SVG, чтобы знак наследовал тему и не давал лишнего запроса.
