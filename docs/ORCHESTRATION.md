# ORCHESTRATION — параллельная разработка

> Карта владения файлами и порядок волн. Обязательна к прочтению любым агентом до первой правки.
>
> Решения: [DECISIONS.md](DECISIONS.md) ADR-020 · Стандарты кода: [CLAUDE.md](CLAUDE.md) · План работ: [ROADMAP.md](ROADMAP.md)

---

## Где мы сейчас

🔴 **Все волны пройдены.** Домен, UI Kit, уведомления, данные, каркас, блоки
лендинга, База знаний, админка и SEO-обвязка собраны и влиты; сайт — лендинг
(ADR-049). Карта волн ниже сохраняется как объяснение того, почему код
разложен именно так, а не как план работ.

Дальше работа идёт точечно: задача — не «волна», а пункт из
[ROADMAP.md](ROADMAP.md) или запись из [BUGS.md](BUGS.md). Правила владения
файлами и запрет на git у агентов остаются в силе — они про среду, а не про
этап.

---

## Правила для агента

Агенты работают **в одном рабочем каталоге одновременно**. Это даёт им видеть код друг друга (страницам нужен UI Kit), но требует дисциплины.

🔴 **Правь только файлы из своего списка владения.** Нужен чужой файл — не правь, сообщи оркестратору в итоговом отчёте.

🔴 **Агент не работает с git. Совсем.** Ни `git add`, ни `git commit`, ни `checkout`, `reset`, `amend`, `rebase`. Всю работу с историей делает оркестратор.

Причина в том, что индекс, HEAD и файл сообщения существуют **в одном экземпляре на репозиторий**. При нескольких агентах это дало: очередь за `.git/index.lock` с коммитами по десять минут, дважды подменённые сообщения коммитов, один аменд поверх чужой работы (ADR-032, ADR-033, ADR-035). Один писатель снимает весь класс проблем разом.

**Что агент делает вместо коммита:** доводит работу до готовности, прогоняет проверки и в отчёте даёт **точный список созданных и изменённых файлов** плюс **текст сообщения коммита**, который написал бы сам. Оркестратор коммитит.

🔴 **Одновременно работает не больше четырёх агентов.** Машина разработчика — восемь ядер; при пяти параллельных прогонах `pnpm check` контейнер упирается в потолок процессора, а однажды словил OOM. Пятый агент не ускоряет работу, он замедляет остальных четырёх.

🔴 **Полный `pnpm check` без нужды не запускать** — он занимает контейнер минутами. Проверять свою зону:

```
docker compose -f docker-compose.dev.yml exec -T web pnpm --filter web exec vitest run <свой путь>
docker compose -f docker-compose.dev.yml exec -T web pnpm --filter web exec tsc --noEmit
```

🔴 **Формат коммита** — Conventional Commits со своим scope, без упоминаний ИИ и соавторства (см. раздел «Коммиты» в [CLAUDE.md](CLAUDE.md)).

🔴 **Вопрос задать некому.** Фоновый агент не может обратиться к пользователю. Всё неочевидное решается по документации; если документация молчит — выбери разумный вариант, реализуй и **явно перечисли принятые допущения в отчёте**, чтобы оркестратор зафиксировал их в журнале решений.

**Перед началом:** прочитать `docs/CLAUDE.md` (инварианты), затем документ своей области. **Перед сдачей:** линт, типы и свои тесты зелёные; у каждого компонента и блока есть story; чек-лист из `CLAUDE.md` пройден.

🔴 **Одноразовые пробы Playwright удаляются самим агентом.** `console.log` в них валит `pnpm lint` правилом `no-console`, и полный прогон падает не на коде, а на мусоре. Это уже случалось: три упавших агента оставили `probe.mts`, `hero-probe.mts` и `hero-slack.mts`, и `pnpm check` у оркестратора упал на них, а не на работе.

---

## Что делает оркестратор после сдачи агента

🔴 **Отчёт агента — не доказательство.** Проверять нужно дерево, а не текст.

- **Агент может умереть посреди задачи.** 28 августа три агента подряд оборвались на ошибке авторизации API, и все три — ровно на шаге приёмки глазами: код дописать успели, посмотреть нет. Последнее сообщение упавшего агента при этом врёт в обе стороны: один остановился на «сейчас подсказка в панели и истории», хотя и подсказку, и истории уже сделал.
- **Доводить приёмку глазами приходится оркестратору,** и она находит то, чего в диффе не видно. В той же волне нашлись два дефекта: у строки сравнения каталога остался `scroll-margin-top`, складывавшийся с новым общим правилом (агент снял его у заголовков статей и пропустил здесь), и резерв под скидку, рассчитанный на 375px и не покрывающий 320px. Оба видны только измерением в браузере.
- **Полный `pnpm check` — после волны, а не вместо неё.** Он ловит мусор и чужие зоны; свою работу агент проверяет сам.

## Git у оркестратора: две ловушки индекса

- 🔴 **Упавший коммит оставляет файлы в индексе.** Хук отклонил коммит — staged-файлы никуда не делись, и **следующий** `git commit` заберёт их с собой. Так в одном коммите оказались правка хука и правка первого экрана. Лечится `git reset` перед новой попыткой; проверяется `git show --stat HEAD` **после каждого** коммита, а не в конце.
- **Scope коммита проверяется commitlint по списку** (`commitlint.config.mjs`): `build` в нём нет, есть `process`. Сообщение с чужим scope отклоняется на `commit-msg`, когда pre-commit уже отработал минуту.

**Конфигурация Storybook** (`apps/web/.storybook/**`) — общая, правит только оркестратор. Stories лежат рядом со своим компонентом и принадлежат его владельцу.

**Что делать нельзя никому, кроме владельца файла:** трогать `package.json`, `tsconfig.json`, `Dockerfile`, `docker-compose.*.yml`, `prisma/schema.prisma`, конфиги линтеров. Нужна новая зависимость — запросить у оркестратора в отчёте, не ставить самому.

---

## Волна 0 — фундамент · последовательно

Выполняется оркестратором в основном чате. Параллелить нельзя: отсюда импортирует всё остальное.

Каркас Next, Docker, линтеры, схема Prisma с миграцией, сиды, токены, шрифты, конфиги тестов.

**Критерий готовности:** `docker compose -f docker-compose.dev.yml up` поднимает сайт через Caddy на локальном домене, база засеяна, `pnpm lint` и `pnpm typecheck` зелёные.

---

## Волна 1 — домен и инфраструктура · 4 агента

| Агент                      | Владеет                                                                                                                                                                                      | Не трогает                                       | Задача                                                                                                                                                  |
| -------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **A · Домен**              | `apps/web/src/entities/*/model.ts`, `apps/web/src/entities/*/lib/**`, `apps/web/src/shared/lib/**`                                                                                           | `server/**`, `app/**`, `shared/ui/**`            | Калькулятор монтажа, `getActivePrice`, подбор по площади, объединение `specs`, рендер разметки статей, слаги, форматирование — чистые функции с тестами |
| **B · UI Kit**             | `apps/web/src/shared/ui/**`, `apps/web/src/shared/styles/**` (кроме `tokens.css`)                                                                                                            | всё остальное                                    | Компоненты из [DESIGN_BRIEF §5](DESIGN_BRIEF.md), адаптив от 320px, обе темы, тесты RTL                                                                 |
| **C · Уведомления**        | `apps/web/src/server/notifications/**`, `apps/web/src/app/api/leads/**`, `apps/web/src/app/api/reviews/**`                                                                                   | `app/api/admin/**`, `apps/web/src/middleware.ts` | Очередь, воркер с ретраями, каналы email и telegram со сменным транспортом, публичные POST с антиспамом и согласием                                     |
| **D · Данные и админ-API** | `apps/web/src/server/repo/**`, `apps/web/src/server/db.ts`, `apps/web/src/server/auth.ts`, `apps/web/src/app/api/admin/**`, `apps/web/src/app/api/settings/**`, `apps/web/src/middleware.ts` | `entities/*/model.ts`, `entities/*/lib/**`       | Слой доступа к данным, cookie-сессия, CRUD по [API.md](API.md), точечная ревалидация                                                                    |

A владеет доменной логикой в `entities/`, D — слоем доступа к данным в `server/repo/`. Репозитории вынесены из `entities/` сознательно: линтер запрещает клиентским слоям импортировать `@/server/*`, а репозиторий по определению ходит в базу.

C и D делят `app/api/`, но разные подкаталоги.

---

## 🔴 Вентиль перед блоковой волной

Ни одна задача на блок не выдаётся, пока не влито **всё** из этого списка. Правило: агент блока не верстает переиспользуемые элементы — если блоку нужен компонент, которого нет, это блокер, а не повод его написать.

Проверено аудитом прототипа. К инвентарю из [DESIGN_BRIEF §5](DESIGN_BRIEF.md) добавлены три компонента, которых там не было:

| Компонент                                          | Кому нужен                 | Почему без него нельзя                      |
| -------------------------------------------------- | -------------------------- | ------------------------------------------- |
| **Drawer / бургер-меню**                           | Шапка                      | Мобильной навигации в прототипе нет вообще  |
| **Table со скроллом и залипающей первой колонкой** | Каталог и сравнение, Цены  | Два блока решали бы одну задачу по-разному  |
| **FileInput с превью и валидацией**                | Форма заявки, Форма отзыва | Иначе две реализации сжатия и проверки типа |

Плюс две единицы, которые выглядят как блоки лендинга, но ими не являются:

- **`features/lead-form`** — форма заявки, секция `#lead` лендинга
- **`features/review-form`** — форма отзыва, секция `#review-form` лендинга

И хук **`useCountUp`** (анимация цифр в hero и «Почему мы») — не компонент, живёт в `shared/lib`, владелец — агент A волны 1.

---

## Волна 2 — интерфейс · 4 агента

| Агент                    | Владеет                                                                                                                     | Задача                                                |
| ------------------------ | --------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| **E · Каркас и главная** | `apps/web/src/app/(site)/layout.tsx`, `apps/web/src/app/(site)/page.tsx`, `apps/web/src/widgets/**`                         | Шапка, футер, виджеты всех секций, сборка главной     |
| **G · База знаний**      | `apps/web/src/app/(site)/knowledge/**`, `apps/web/src/app/(site)/privacy/**`                                                | Листинг, страница статьи, политика                    |
| **H · Админка**          | `apps/web/src/app/(admin)/**`                                                                                               | Все разделы, включая «Компания» и «Заявки»            |
| **I · SEO-обвязка**      | `apps/web/src/app/sitemap.ts`, `apps/web/src/app/robots.ts`, `apps/web/src/shared/seo/**`, `apps/web/src/app/not-found.tsx` | Метаданные, JSON-LD, карта сайта, хлебные крошки, 404 |

E владеет всеми виджетами единолично — иначе волна 3 не сможет собирать из них страницы.

I не трогает `shared/lib/**` (владение A волны 1); сборщики разметки живут в отдельном `shared/seo/**`.

---

## Волна 3 — блоки лендинга · worktree, по одному блоку на агента

🔴 **Изоляция — по файлам, как в волне 1** (ADR-031: worktree отменён, среда выполнения одна на репозиторий). Каждый блок — свой каталог `widgets/<block>/`, пересечений нет.

Вентиль сохраняется: агент сдаёт виджет, который **сам по себе на сайте не появляется**. Оркестратор смотрит блок в Storybook на 320, 375, 768 и 1200 в обеих темах, прогоняет тесты и только после этого подключает к странице. Вливание в основной поток — это и есть сборка страницы.

Каждый блок — свой каталог `apps/web/src/widgets/<block>/`, свои stories, свои тесты, своя адаптивная вёрстка от 320px.

### 🔴 Блок не ходит в базу

Виджет — **представление**: принимает готовые данные пропсами и рисует их. Запрос в репозиторий делает страница, она же передаёт результат вниз. Причин две:

- правило зависимостей: `widgets` не может импортировать `@/server/*`, это проверяет линтер;
- блок обязан рисоваться в Storybook, где никакой базы нет. Блок, которому нужен живой Postgres, невозможно ни посмотреть, ни принять.

Поэтому у каждого блока в stories лежат фикстуры — они же документируют, какие данные блок ждёт.

### Сборку страницы делает оркестратор

Агент блока не трогает `app/(site)/page.tsx` — иначе девять агентов правили бы один файл. Он сдаёт виджет с его контрактом пропсов; оркестратор при вливании подключает блок к странице и связывает с репозиторием.

Отсюда порядок: **контент появляется на сайте постепенно**, по мере приёмки блоков, а не одномоментно в конце волны.

| #   | Задача             | Секции прототипа                                                        |
| --- | ------------------ | ----------------------------------------------------------------------- |
| 1   | Шапка и футер      | Шапка с логотипом, бургер-меню, переключатель темы; футер с реквизитами |
| 2   | Первый экран       | Hero, подбор по площади, живой фон, счётчики                            |
| 3   | Доверие            | Полоса доверия, Услуги, Почему в Туле выбирают нас                      |
| 4   | Каталог            | Витрина моделей со скидками, таблица сравнения                          |
| 5   | Цены               | Таблица цен по классам, калькулятор монтажа                             |
| 6   | Монтаж             | Четыре шага, таймлайн дня монтажа, экономия инвертора                   |
| 7   | Честность          | Почему монтаж стоит 6 000, Как обманывают при установке                 |
| 8   | Сервис             | Диагностика по симптомам                                                |
| 9   | Справка и контакты | Отзывы, тизер Базы знаний, FAQ, контакты с картой                       |

Задача 9 самая объёмная — при постановке может быть разделена надвое.

Форма заявки и форма отзыва в список **не входят**: они сделаны в волне 1 как переиспользуемые фичи, блоки только встраивают их.

---

## Волна 4 — выкладка · 1 агент

| Агент              | Владеет                                                                 | Задача                                          |
| ------------------ | ----------------------------------------------------------------------- | ----------------------------------------------- |
| **L · Прод и e2e** | `docker-compose.prod.yml`, `Caddyfile`, `.github/**`, `apps/web/e2e/**` | Боевая сборка, CI, сквозные сценарии Playwright |

Агент кластера страниц (**K**) из волны исключён: сайт стал лендингом, отдельных
страниц под разделы больше нет (ADR-049).

---

## Между волнами

Оркестратор прогоняет линт, типы, тесты и `/code-review`, сводит допущения агентов в [DECISIONS.md](DECISIONS.md) и только потом запускает следующую волну.

Волна не считается закрытой, пока сборка не зелёная. Чинить — тем же агентом, который писал код, а не следующей волной.
