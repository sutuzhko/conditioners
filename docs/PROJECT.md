# ТулаКлимат — проектный документ

> Продуктовый и доменный контекст. Инженерные правила: [CLAUDE.md](CLAUDE.md) · SEO: [SEO.md](SEO.md) · API: [API.md](API.md) · Инфраструктура: [TECH_DECISIONS.md](TECH_DECISIONS.md)

---

## Часть 0. Что это

Сайт компании, которая в Туле и Тульской области продаёт кондиционеры, ставит их под ключ и обслуживает.

Три источника денег, в порядке важности:

1. **Монтаж** — основная маржа. Продажа техники часто идёт «в ноль», зарабатывают на установке.
2. **Сервис** — чистка, заправка, ремонт. Круглогодичный поток, сглаживает сезонность.
3. **ТО по подписке** — ежегодное обслуживание ранее установленных кондиционеров.

**Сезонность экстремальная.** С мая по август — вал заявок, зимой — почти тишина. Отсюда два следствия: сайт должен быть в топе **до** начала сезона (индексация занимает месяцы, значит работы стартуют осенью-зимой), и зимний контент должен продавать обогрев инвертором и межсезонное ТО по сниженной цене.

**Аудитория:** частные лица, квартиры и дома в Туле, 30–55 лет, с телефона, в жару, в состоянии «нужно вчера». Второй сегмент — небольшие офисы и магазины.

**Конкурентное позиционирование:** честность сметы. Весь контент построен вокруг того, что клиента обманывают на каждом шагу («дозаправка фреона», «продувка вместо вакуумации», тонкая труба, гарантия на словах) — и что мы показываем цену до выезда. Это не маркетинговая обёртка, а обязательство: см. красные линии в [CLAUDE.md](CLAUDE.md).

---

## Часть 1. Источник дизайна

Дизайн пришёл из Claude Design как HTML/CSS/JS-прототип. Исходники являются **эталоном визуала**.

🔴 **В репозитории их нет** — каталог `design/` в `.gitignore` (внутри, помимо прочего, скомпрометированный токен бота). Они лежат локально у разработчика, источник — архив `Сайт кондиционеров Тула-handoff.zip`. Всё существенное из них перенесено в документацию, так что работать можно и без доступа к архиву.

| Файл                                        | Что это                                                                                                         |
| ------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| `design/Кондиционеры Тула.dc.html`          | Главная: все секции сайта                                                                                       |
| `design/База знаний.dc.html`                | Список статей и страница статьи                                                                                 |
| `design/Админ-панель.dc.html`               | Админка первой версии: Цены, Модели, Контакты, Юрлицо, База знаний, Отзывы                                      |
| `design/Админка CRM.dc.html`                | 🆕 Панель как CRM: обзор, календарь, обращения, заказы, клиенты, монтажники, профиль. Разбор — [CRM.md](CRM.md) |
| `design/crm-data.js`                        | 🆕 Мок-хранилище CRM (`tk-crm-v3`): наряды, клиенты, монтажники, чеклисты                                       |
| `design/Кондиционеры Тула v2.dc.html`       | 🆕 Главная, вторая версия: форма отзыва переехала в модальное окно                                              |
| `design/data.js`                            | Мок-хранилище на localStorage — основа для сидов БД                                                             |
| `design/API-контракт.md`                    | Исходный контракт, от которого отталкивается [API.md](API.md)                                                   |
| `design/support.js`, `design/image-slot.js` | Рантайм самого дизайн-инструмента. **В продакшн не переносится**                                                |

Прототип воспроизводится визуально, а не структурно: его внутреннее устройство (инлайн-стили, класс `DCLogic`, `renderVals`) к нашей архитектуре отношения не имеет.

---

## Часть 2. Домен

### 2.1. Модель (товар)

Кондиционер в каталоге. Ключевая особенность — **произвольный набор характеристик**: список пар «название → значение», который владелец задаёт в админке. Таблица сравнения строится как объединение ключей всех видимых моделей, отсутствующее значение выводится прочерком.

Так владелец может завести характеристику «Wi-Fi управление» только у одной модели — и таблица сама вырастет на строку. Фиксированного набора полей характеристик не существует ни в схеме БД, ни в коде.

### 2.1.1. Справочник характеристик

Набор характеристик остаётся произвольным (инвариант 6), но у него появился
**порядок и группы** — они живут в настройках под ключом `specs` и правятся
владельцем (ADR-094).

Справочник ничего не ограничивает. Он подсказывает названия в редакторе
модели, раскладывает характеристики по группам в карточке товара и задаёт
порядок строк в таблице сравнения. Характеристика, которой в нём нет,
работает как прежде и показывается в группе «Прочее».

Сидируется отраслевым набором из восьми групп: основное, энергоэффективность,
шум и воздух, рабочие температуры, монтаж, габариты и вес, электрика,
возможности. Монтажные параметры (диаметры, длина трассы, хладагент) в
потребительских описаниях обычно не публикуют — здесь они нужны: по ним
собирается смета трассы и чеклист выезда.

### 2.2. Класс мощности

Сквозное понятие: `07`, `09`, `12`, `18` — тысячи BTU. Связывает модель, цену монтажа и калькулятор. Класс определяет площадь: 07 → до 20 м², 09 → до 27 м², 12 → до 35 м², 18 → до 50 м².

### 2.3. Подбор модели по площади

Алгоритм из прототипа, переносится дословно:

```
видимые модели, отсортированные по areaMax ↑
берём первую, у которой areaMax >= указанной площади
если такой нет — берём последнюю (самую мощную)
если тип помещения = «Офис» и есть модель мощнее — сдвигаемся на одну вверх
```

Сдвиг для офиса — не прихоть: техника и люди дают дополнительный приток тепла.

### 2.4. Калькулятор монтажа

```
итог = ( база(класс)
       + max(0, трасса − 3) × trassaPerM
       + (этаж 10+ ? heightWorks : 0)
       + (штробление ? трасса × shtrobPerM : 0)
       ) × количество
```

Все коэффициенты — из БД, редактируются в админке. Три метра трассы входят в базу — это часть обещания «без скрытых доплат».

🔴 Чистая функция в `entities/price`, покрытая тестами. Ни одна цифра не зашита в компонент.

### 2.5. Отзыв

Приходит с сайта со статусом `pending`. Модератор переводит в `approved` (публикуется), `rejected` (снят) или `archived` (позвонить клиенту, выяснить, что улучшить).

🔴 Текст отзыва не редактируется никогда и никем.

К отзыву можно приложить два разных снимка, и путать их нельзя: фотография автора (`avatar`) и снимок с места установки (`photo`) — по второму видно и аккуратность бригады, и мусор, если он остался. Оба необязательны.

Телефон автор не оставляет, но согласие на обработку персданных даёт — имя и фотография тоже персональные данные (152-ФЗ), и факт согласия пишется в `consentAt`.

### 2.6. Заявка

Главная ценность сайта. Поля: имя, телефон, тема (монтаж / сервис / ТО / консультация), тип помещения, количество, удобное время звонка, адрес или район, комментарий, фото места установки.

🔴 Сохраняется в БД **до** любой попытки уведомления. Уведомление — отдельная асинхронная сущность, см. [TECH_DECISIONS.md](TECH_DECISIONS.md).

Дополнительно пишем то, чего не было в прототипе: страницу-источник, UTM-метки, реферер, факт и время согласия на обработку персданных.

### 2.7. Статья Базы знаний

Плоский текст с лёгкой разметкой, которую рендерит фронт: `## заголовок`, `### подзаголовок`, `- пункт`, `> врезка`, `**жирный**`, блоки разделяются пустой строкой.

Свой мини-формат вместо Markdown-редактора выбран сознательно: владелец правит текст в обычном textarea, без визуального редактора и без риска сломать вёрстку вставкой из Word. Рендер — чистая функция с тестами.

### 2.8. Дело в календаре

Заявка отвечает на вопрос «кто обратился». Что происходит дальше — когда выехать на замер, кому перезвонить в четверг, когда монтаж, — живёт отдельной сущностью: вид (звонок, замер, монтаж, обслуживание, встреча, заметка), момент времени, клиент, телефон, адрес, заметка, статус (запланировано, сделано, отменено).

Клиент задаётся строками, а не ссылкой на заявку: половина работ приходит по звонку и сарафану, и такое дело обязано попадать в календарь без выдуманной заявки на сайте. Связь с заявкой есть, но необязательная (ADR-080).

🔴 Время всегда московское: пояс задаёт город работ, а не браузер того, кто открыл панель.

### 2.8.1. Клиент

Заявка отвечает на вопрос «кто обратился», клиент — «с кем мы работаем». Это разные сроки жизни: обращение закрывается статусом, человек остаётся — у него адрес, история работ и техника, которую ему поставили. Поля: имя, телефон, адрес, заметка.

🔴 **Телефон — ключ.** Он хранится дважды: как ввели (владелец узнаёт свою запись и диктует номер привычно) и нормализованным — только цифры, код приведён к 7. Дедупликация и поиск идут по нормализованному: «+7 (910) 155-24-68», «8 910 155 24 68» и «9101552468» — один человек, и второй карточки у него быть не должно (ADR-105).

Клиент заводится **вручную**: из карточки обращения кнопкой «В клиенты» либо прямо в разделе. Автоматически при приёме заявки — нет: в постоянную базу с адресами попадали бы все, кто спросил цену, включая спам. Это и лишние персональные данные, и список, в котором не найти настоящего клиента.

Связь с обращением записывается полем, а не подбирается по совпадению телефонов: номер правится, и история, склеенная по строкам, разъехалась бы при первой же правке. Удаление карточки — в том числе по требованию субъекта ПДн — обращения не трогает: у них своё согласие и свой срок хранения.

### 2.9. Скидка

Маркетинговый инструмент для сезона и межсезонья. Задаётся у товара в админке:

| Поле                  | Смысл                                                                                                    |
| --------------------- | -------------------------------------------------------------------------------------------------------- |
| `salePrice`           | конечная цена со скидкой. Пусто = скидки нет                                                             |
| `saleFrom` / `saleTo` | период действия. Пусто = бессрочно                                                                       |
| `saleLabel`           | необязательная подпись: «Осенняя цена», «Последняя в наличии». Если пусто — рисуется вычисленный процент |

Процент считается сам: `round((1 − salePrice / priceNum) × 100)`. Скидка активна, только если `salePrice` заполнена **и** текущая дата внутри периода — расчёт живёт в одной функции `getActivePrice(product)`, которой пользуются и карточка, и каталог, и разметка, и калькулятор.

**Почему конечная цена, а не процент.** Владелец мыслит ценой на витрине: «поставлю 34 900» понятнее, чем «поставлю 12%», и даёт круглую цифру вместо 34 892 ₽. Процент выводится, а не вводится.

🔴 **Ограничение честности.** Перечёркнутая цена — только та, по которой товар действительно продавался раньше. Нарисованная «старая цена» ради красивой плашки — введение в заблуждение по ЗоЗПП и ФЗ «О рекламе», и Яндекс за это тоже наказывает. Правило то же, что и с отзывами: маркетинг не отменяет позиционирование «честно о цене», на котором построен весь сайт.

Окончание периода снимает скидку само, без участия владельца. В разметке `Offer` `saleTo` отдаётся как `priceValidUntil`.

---

---

## Часть 3. Модель данных

Ниже — смысловой срез: поля, о которых нужно договориться. Настоящий источник
правды — `apps/web/prisma/schema.prisma`; служебные `createdAt` / `updatedAt`
и обратные стороны связей здесь опущены, чтобы не мешали читать.

```prisma
model Product {                       // «Модель» в терминах админки
  id        String   @id @default(cuid())
  slug      String   @unique          // стабильный идентификатор модели в API и админке
  badge     String                    // класс мощности: «09»
  name      String                    // «Сплит-система 09» или реальное имя модели
  brand     String?                   // производитель
  sku       String?                   // артикул модели у производителя
  areaMax   Int
  tag       String?                   // короткая метка: «инвертор», «тихая, для спальни»
  priceNum  Int                       // обычная цена
  salePrice Int?                      // цена со скидкой; null = скидки нет
  saleFrom  DateTime?
  saleTo    DateTime?
  saleLabel String?                   // «Осенняя цена»; пусто — рисуем вычисленный процент
  link      String?                   // внешняя ссылка на товар у поставщика
  visible   Boolean  @default(true)
  sort      Int      @default(0)
  seoTitle       String?
  seoDescription String?
  photos    ProductPhoto[]
  specs     ProductSpec[]
}

model ProductPhoto {
  id        String  @id @default(cuid())
  productId String
  url       String
  alt       String?
  isMain    Boolean @default(false)
  sort      Int     @default(0)
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)
}

model ProductSpec {
  id        String  @id @default(cuid())
  productId String
  k         String
  v         String
  sort      Int     @default(0)
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)
}

model PriceRow {                      // цена монтажа по классу
  cls   String @id                    // «07» | «09» | «12» | «18»
  power String
  area  String
  price Int
  term  String
  sort  Int    @default(0)
}

model Setting {                       // все данные о компании и настройки — по ключу
  key   String @id
  value Json
}

model Article {
  id        String   @id @default(cuid())
  slug      String   @unique
  title     String
  category  String
  date      DateTime
  minutes   Int
  cover     String?
  excerpt   String
  body      String
  published Boolean  @default(false)
  seoTitle       String?
  seoDescription String?
  updatedAt DateTime @updatedAt
}

model Review {
  id        String       @id @default(cuid())
  name      String
  rating    Int
  text      String       // 🔴 неизменяем
  photo     String?      // место установки
  avatar    String?      // сам автор
  status    ReviewStatus @default(PENDING)
  consentAt DateTime?    // 🔴 согласие на обработку персданных
  createdAt DateTime     @default(now())
}

model Lead {
  id        String   @id @default(cuid())
  name      String
  phone     String
  topic     String
  place     String?
  qty       String?
  callTime  String?
  address   String?
  comment   String?
  photo     String?
  sourceUrl String?
  referrer  String?
  utm       Json?
  consentAt DateTime          // 🔴 доказательство согласия
  status    LeadStatus @default(NEW)
  managerComment String?      // заметка менеджера; данные клиента не правятся
  clientId  String?           // в кого выросло обращение; SetNull при удалении (ADR-105)
  createdAt DateTime   @default(now())
}

model Client {                        // человек, с которым работают (ADR-105)
  id        String   @id @default(cuid())
  name      String
  phone     String                    // как ввели: владелец узнаёт свою запись
  phoneKey  String   @unique          // только цифры, код приведён к 7 — ключ дедупликации
  address   String?
  note      String?
  createdAt DateTime @default(now())
}

model CrmEvent {                       // дело в календаре работ (ADR-080)
  id          String         @id @default(cuid())
  kind        CrmEventKind   // CALL | MEASURE | INSTALL | SERVICE | MEETING | NOTE
  status      CrmEventStatus @default(PLANNED)
  at          DateTime       // момент в UTC, показывается в московском
  clientName  String
  clientPhone String?
  address     String?
  note        String?
  leadId      String?        // заявка, из которой выросло дело; onDelete: SetNull
  createdAt   DateTime       @default(now())
  updatedAt   DateTime       @updatedAt
}

model Notification {
  id        String   @id @default(cuid())
  channel   String   // «email» | «telegram»
  kind      String   // «lead» | «review» | «to-reminder»
  payload   Json
  status    String   @default("pending")
  attempts  Int      @default(0)
  lastError String?
  nextTryAt DateTime @default(now())   // когда воркер возьмёт запись снова
  createdAt DateTime @default(now())
  sentAt    DateTime?
}

enum AdminRole { OWNER | INSTALLER }   // ADR-092

model AdminUser {                   // и владелец, и монтажник: оба входят в панель
  id           String    @id @default(cuid())
  login        String    @unique
  passwordHash String              // Argon2id
  role         AdminRole @default(OWNER)
  name         String?             // показывается в шапке и в наряде
  phone        String?
  active       Boolean   @default(true)  // отключение закрывает вход и открытые сессии
  lastLoginAt  DateTime?
}

model InstallerNote {               // заметки владельца о монтажнике; тот их не видит
  id        String   @id @default(cuid())
  userId    String
  text      String
  createdAt DateTime @default(now())
}

model Session {                     // сессия админки
  id        String   @id @default(cuid())
  userId    String
  tokenHash String                  // 🔴 в базе хеш, а не сам токен из cookie
  expiresAt DateTime
}

model RateLimit {                   // счётчик обращений по IP для публичных POST
  id       String   @id @default(cuid())
  key      String                   // ip + endpoint
  windowAt DateTime                 // начало окна; уникальна пара key + windowAt
  hits     Int      @default(1)
}
```

### Ключи `Setting`

🔴 Здесь живёт **всё, что в прототипе было выдумано и зашито в разметку**. Ни один из этих фактов не имеет права появиться в коде — см. ADR-009.

| Ключ            | Содержимое                                                                                         | Где используется                            |
| --------------- | -------------------------------------------------------------------------------------------------- | ------------------------------------------- |
| `company`       | название, юридическое и торговое, слоган, подпись под логотипом, год основания                     | шапка, футер, `title`, `Organization`       |
| `contacts`      | телефоны (несколько), e-mail, мессенджеры (WhatsApp, Telegram), часы работы                        | шапка, контакты, футер, `HVACBusiness`      |
| `address`       | город, улица и дом, офис, индекс, страна — **по частям**, а не строкой                             | контакты, футер, `PostalAddress`            |
| `geo`           | широта и долгота                                                                                   | `GeoCoordinates`, карта                     |
| `area`          | регион обслуживания, список районов и городов области                                              | `areaServed`, гео-страницы                  |
| `legal`         | форма (ИП/ООО) и **её** набор реквизитов — состав по §5.1, проверка разрядом по §5.2               | футер «Реквизиты», политика, оферта         |
| `extras`        | ставки калькулятора: ₽/м трассы, ₽/м штробления, высотные работы                                   | калькулятор, подписи ставок                 |
| `warranty`      | сроки и условия гарантии на монтаж и технику                                                       | страницы услуг, FAQ                         |
| `payment`       | способы оплаты, работа с НДС, рассрочка                                                            | футер, карточка товара, `Offer`             |
| `social`        | ссылки на соцсети и карты                                                                          | футер, `sameAs`                             |
| `seo`           | `title`/`description` главной, дефолтная OG-картинка, суффикс бренда в `title`                     | метаданные всех страниц                     |
| `achievements`  | цифры первого экрана: «1200+ установок», «3 года гарантии» — значение, суффикс и подпись (ADR-052) | первый экран                                |
| `specs`         | справочник характеристик: группы и типовые поля (ADR-094)                                          | карточка товара, сравнение, редактор модели |
| `integrations`  | счётчик Метрики, кнопки мессенджеров                                                               | шапка, футер, скрипты аналитики             |
| `notifications` | куда уходит сообщение о новой заявке: включённость каналов и адресаты (ADR-058)                    | очередь уведомлений                         |

Адрес хранится по частям сознательно: `PostalAddress` в JSON-LD требует отдельных полей, а Яндекс.Бизнес сверяет их построчно. Из частей всегда можно собрать строку, из строки части — нет.

Канонический домен — исключение: он остаётся в `SITE_URL` (ENV), потому что смена домена это переезд с редиректами и переиндексацией, а не правка поля в форме.

### Сиды

Берутся из `design/data.js` — структуры совпадают с контрактом один в один.

Два исключения:

- **Отзывы не сидируем** — стартуем с пустым разделом (инвариант 10, ADR-012).
- **Данные компании сидируются как явные заглушки** вида «ЗАПОЛНИТЕ В АДМИНКЕ», а не правдоподобные выдумки. Заглушка, которую видно, уедет в прод один раз; правдоподобный «+7 (4872) 00-00-00» уедет незаметно и останется там на год.

---

## Часть 4. Что редактируется без разработчика

🔴 **Принцип: разработчик не нужен ни для одного изменения контента.** Если владельцу приходится звонить программисту, чтобы поменять телефон, цену или добавить товар, — это дефект архитектуры, а не «маленькая доработка».

| Раздел админки                  | Данные                                                                                                                                                                                                              | Где на сайте                                                                  |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| **Компания** 🆕                 | название и слоган, телефоны, мессенджеры, e-mail, часы работы, адрес по частям, координаты, регион и районы обслуживания, соцсети, тексты гарантии и оплаты, `title`/`description` главной, OG-картинка             | шапка, футер, контакты, метаданные, весь JSON-LD                              |
| **Реквизиты**                   | форма (ИП/ООО), наименование, ИНН, ОГРН/ОГРНИП, юридический адрес                                                                                                                                                   | футер «Реквизиты», политика, оферта                                           |
| **Цены**                        | монтаж по классам 07/09/12/18, ₽/м трассы, ₽/м штробления, высотные работы                                                                                                                                          | таблица цен, калькулятор, подписи ставок                                      |
| **Каталог**                     | название, бренд, артикул, слаг, класс, площадь, цена, **скидка и её период**, несколько фото с выбором главного, внешняя ссылка, произвольные характеристики, метка, видимость, порядок, свои `title`/`description` | каталог, карточка товара, подбор в hero, таблица сравнения, `Product`/`Offer` |
| **Справочник характеристик** 🆕 | группы и типовые названия характеристик, единицы измерения, подсказки                                                                                                                                               | группы в карточке товара, порядок строк сравнения, подсказки в редакторе      |
| **База знаний**                 | статьи: CRUD, обложка, публикация, свои `title`/`description`                                                                                                                                                       | раздел статей, тизер на главной                                               |
| **Отзывы**                      | модерация статусов (текст неизменяем)                                                                                                                                                                               | секция отзывов, страница отзывов                                              |
| **Заявки** 🆕                   | просмотр, фильтр, статус, комментарий менеджера                                                                                                                                                                     | нигде — внутренний раздел                                                     |
| **Монтажники** 🆕               | учётные записи команды: имя, логин, телефон, пароль, доступ в панель, заметки владельца                                                                                                                             | нигде — внутренний раздел (ADR-092)                                           |
| **Профиль** 🆕                  | своё имя, телефон, смена пароля, тема интерфейса                                                                                                                                                                    | нигде — внутренний раздел                                                     |

Разделов «Компания» и «Заявки» в исходном дизайне нет: данные компании были зашиты в разметку, заявки жили только в Telegram. Оба добавляем в том же визуальном языке, что остальные разделы админки.

Форма регистрации задаёт **состав** реквизитов, а не одну подпись: у
предпринимателя нет КПП и руководителя, у общества — органа регистрации (§5.1,
ADR-112). Поля чужой формы не прячутся — их не существует, и запрос с таким
полем схема отвергает. Подпись `ogrn` тоже зависит от формы: «ОГРНИП» у ИП,
«ОГРН» у ООО.

## Часть 5. Юридический контур

| Требование                  | Что делаем                                                                                                                                                                                 |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **152-ФЗ, локализация**     | БД и файлы на сервере в РФ. Зарубежный SaaS для хранения ПДн запрещён                                                                                                                      |
| **152-ФЗ, согласие**        | Отдельный чекбокс в каждой форме (не предустановлен), ссылка на политику. Факт и время согласия пишутся в `Lead.consentAt`                                                                 |
| **152-ФЗ, политика**        | Страница «Политика обработки персональных данных», доступная из футера и из каждой формы                                                                                                   |
| **152-ФЗ, уведомление РКН** | Уведомление в Роскомнадзор об обработке ПДн — до запуска. Если уведомления в Telegram идут через зарубежный узел, это трансграничная передача и она указывается в уведомлении и в политике |
| **ФЗ «О рекламе»**          | Никаких «лучшие в Туле», «№1» без подтверждения. Никаких фейковых отзывов и рейтингов                                                                                                      |
| **ЗоЗПП**                   | Реквизиты продавца, порядок оплаты, гарантия и порядок возврата — на сайте, доступны с любой страницы                                                                                      |
| **Цены**                    | Указывать «от», если конечная зависит от объекта. Валюта — рубли, НДС-статус явно указан                                                                                                   |
| **Cookie**                  | Метрика ставит cookie → информирующий баннер со ссылкой на политику                                                                                                                        |

### 5.1. Форма регистрации задаёт состав реквизитов

> ⚠️ Ниже — разбор для проектирования полей, а не юридическое заключение.
> Перед запуском состав публикуемых реквизитов и оформление монтажников
> подтверждает бухгалтер или юрист владельца. Ссылки на нормы даны, чтобы
> было что проверять, а не чтобы заменить проверку.

Компания бывает **ИП** или **ООО** — третьего варианта в выборе нет
(решение 26 августа): самозанятый не вправе перепродавать чужой товар
(ФЗ-422), а продажа техники — суть проекта.

| Поле                          | ИП                     | ООО              | На сайте | Зачем                                       |
| ----------------------------- | ---------------------- | ---------------- | -------- | ------------------------------------------- |
| Наименование                  | ФИО полностью          | полное фирменное | да       | ЗоЗПП ст. 9 — потребитель знает, с кем дело |
| Сокращённое наименование      | —                      | «ООО „…“»        | да       | подпись в футере и в `Organization`         |
| ИНН                           | 12 цифр                | 10 цифр          | да       | однозначная идентификация продавца          |
| КПП                           | **нет**                | 9 цифр           | нет      | счета и платёжные документы, не витрина     |
| ОГРН / ОГРНИП                 | ОГРНИП, 15             | ОГРН, 13         | да       | ЗоЗПП ст. 9, Правила продажи (ПП РФ № 2463) |
| Дата и орган регистрации      | да                     | —                | да       | для ИП ст. 9 требует прямо                  |
| Юридический адрес             | адрес регистрации, ПДн | место нахождения | ООО — да | место нахождения продавца                   |
| Руководитель (ФИО, должность) | —                      | да               | нет      | договоры и оферта                           |
| Банковские реквизиты          | опционально            | опционально      | **нет**  | счета; на витрине им делать нечего          |

🔴 **Про адрес ИП.** Место регистрации ИП — это, как правило, его домашний
адрес, и это персональные данные. Публиковать домашний адрес не требуется:
потребителю показывается фактический адрес приёма клиентов и режим работы
(они уже есть в `address` и `contacts`), а регистрационные сведения — это
наименование, ОГРНИП и ИНН. Поэтому у формы «ИП» поле юридического адреса
на сайт не выводится, даже когда заполнено.

### 5.2. Реквизиты проверяются машиной, а не глазами

ИНН, ОГРН, ОГРНИП и КПП имеют фиксированную длину и контрольные разряды —
опечатка в них вычисляется арифметически. Реквизиты видны на сайте и
попадают в политику обработки ПДн: ошибка в цифре живёт там годами и
всплывает в самый неудачный момент. Значит схема реквизитов проверяет не
«поле не пустое», а длину и контрольную сумму — Zod-схема с `refine`, тесты
на настоящих и битых номерах.

Заодно это закрывает расхождение: ИНН из 10 цифр у формы «ИП» — не опечатка
владельца, а признак того, что выбрана не та форма.

### 5.3. Что из реквизитов обязано быть на сайте

ЗоЗПП ст. 9 и Правила продажи товаров (ПП РФ № 2463) требуют довести до
потребителя наименование продавца, сведения о регистрации, адрес и режим
работы — до заключения договора и с любой страницы. Сейчас это футер
(`widgets/footer`), и он же остаётся местом правды.

Реквизиты в политике обработки ПДн и в футере — **одни и те же данные из
`legal`**. Оператор персональных данных в политике не имеет права
отличаться от продавца в футере: это одно лицо, и расхождение — готовый
вопрос от проверяющего. Хардкод реквизитов в тексте политики запрещён
инвариантом 8 ровно поэтому.

### 5.4. Оформление монтажников — три режима, разные обязанности

Хранится у учётной записи (решение 26 августа). Разница не косметическая:

| Режим                 | Кто платит налоги            | Что требуется от компании                              | Риск                                        |
| --------------------- | ---------------------------- | ------------------------------------------------------ | ------------------------------------------- |
| **Самозанятый** (НПД) | сам, через «Мой налог»       | чек на каждую выплату — без него расход не подтверждён | статус слетел → доначисления НДФЛ и взносов |
| **ГПХ**               | компания как налоговый агент | НДФЛ, страховые взносы, акт на каждую работу           | переквалификация в трудовые отношения       |
| **Трудовой договор**  | компания                     | полный кадровый пакет, график, отпуск                  | —                                           |

Что из этого важно системе, а не бухгалтеру:

- **Самозанятость проверяема.** Статус утрачивается при превышении лимита
  дохода или снятии с учёта, и на дату выплаты он может быть уже не тот. У
  ФНС есть открытый сервис проверки статуса по ИНН и дате — поэтому у
  монтажника-самозанятого ИНН нужен не «для порядка», а чтобы проверку
  можно было сделать вообще.
- **Бывший работник — нельзя.** Услуги бывшему работодателю в режиме НПД
  недоступны в течение двух лет после увольнения (ФЗ-422). Если человек
  раньше работал по трудовому договору, переключение его на «самозанятый»
  — не безобидная правка поля.
- **Чек — условие расхода.** Раз выплаты монтажникам когда-нибудь появятся
  в системе ([CRM.md §8.4](CRM.md)), к выплате самозанятому прикладывается
  чек, а к выплате по ГПХ — акт.

### 5.5. Чего о сотруднике не храним

Тип оформления, ИНН (для самозанятого и ИП), номер и дата договора, даты
начала и окончания — этого хватает, чтобы понимать, как человек оформлен, и
проверить его статус.

Паспортные данные, СНИЛС, адрес регистрации, сканы документов в системе не
хранятся: это кадровый учёт, он живёт у бухгалтера. Каждое такое поле —
персональные данные работника, а значит основание, срок хранения и
ответственность по 152-ФЗ. Понадобится — заводится отдельным решением с
обоснованием, а не «пусть полежит, вдруг пригодится».

Публично оформление монтажника не показывается никогда.

### 5.6. Что добавится, когда каталог дорастёт до магазина

Пока сайт принимает заявки, обязательства ограничены разделом выше. Продажа
через сайт добавляет к ним оферту, порядок оплаты и порядок возврата, а
бытовой кондиционер — технически сложный товар, и правила возврата для него
отдельные. Это разбирается с юристом до первой кнопки «Купить», а не после.

---

## Часть 6. Что нужно от владельца

Ключевое: **это не блокеры разработки**. Всё перечисленное заполняется через админку, без выкладки и без участия разработчика (ADR-009, ADR-010). Код пишется на явных заглушках вида «ЗАПОЛНИТЕ В АДМИНКЕ» и не ждёт контента.

Блокером эти пункты становятся только на границе запуска — сайт с заглушкой вместо телефона публиковать нельзя.

### Заполняется в админке, до запуска

| #   | Что                                                          | Раздел админки |
| --- | ------------------------------------------------------------ | -------------- |
| 1   | Название и слоган компании                                   | Компания       |
| 2   | Телефоны, e-mail, мессенджеры, часы работы                   | Компания       |
| 3   | Адрес офиса по частям + координаты для карты и JSON-LD       | Компания       |
| 4   | Регион и районы обслуживания                                 | Компания       |
| 5   | Реквизиты: форма, наименование, ИНН, ОГРН/ОГРНИП, юр. адрес  | Реквизиты      |
| 6   | Реальный прайс на монтаж и ставки допуслуг                   | Цены           |
| 7   | Реальный каталог: бренды, модели, фото, цены, характеристики | Каталог        |
| 8   | Первые статьи Базы знаний                                    | База знаний    |

🔴 **NAP-данные** (название, адрес, телефон) должны совпадать с карточкой в Яндекс.Бизнесе **до символа**. Яндекс сверяет их и наказывает расхождения в локальной выдаче. Именно поэтому они хранятся в одном месте и подставляются везде из него — вручную рассинхронизировать их невозможно.

### Требует действий вне сайта

| #   | Что                                                                                                                                                                                           | Кто                   |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------- |
| 9   | Домен: купить, направить DNS на VPS. Единственный параметр не из админки — живёт в `SITE_URL`                                                                                                 | владелец + разработка |
| 10  | 🔴 Telegram: **старый токен из прототипа скомпрометирован** — лежал открытым в handoff-архиве и уходил в клиентский бандл. Отозвать в BotFather, выпустить новый, передать безопасным каналом | владелец              |
| 11  | Почтовый ящик на домене для SMTP-уведомлений                                                                                                                                                  | владелец              |
| 12  | Доступы: Яндекс.Вебмастер, Метрика, Яндекс.Бизнес                                                                                                                                             | владелец              |
| 13  | Проверка доступности `api.telegram.org` с боевого VPS, выбор транспорта                                                                                                                       | разработка            |
| 14  | Решение по гео-страницам: есть ли реальная фактура по районам                                                                                                                                 | совместно             |

### Чего не будет никогда

- **Выдуманных отзывов.** Раздел стартует пустым, наполняется через форму (ADR-012).
- **Нарисованных «старых цен».** Перечёркнутая цена — только реально действовавшая (ADR-011).
- **Правдоподобных заглушек** в данных компании. Заглушка должна быть заметна, иначе уедет в прод.
