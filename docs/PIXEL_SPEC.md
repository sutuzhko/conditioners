# PIXEL_SPEC — сверка с макетом

> Значения **извлечены из прототипа** `design/Кондиционеры Тула.dc.html`, а не восстановлены на глаз. Это опорная таблица для сверки: агент проверяет свой блок по ней, а не по впечатлению от макета.
>
> Токены и правила: [DESIGN_BRIEF.md](DESIGN_BRIEF.md) · Решения: [DECISIONS.md](DECISIONS.md)

---

## 🔴 Разрешённые отступления — их НЕ откатывать

Эти расхождения с макетом приняты сознательно и зафиксированы решениями. Агент, который «починит» их обратно, сломает проект.

| Отступление                                                              | Причина                                                                                                                                                                                                                                           |
| ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Onest вместо Space Grotesk, JetBrains Mono вместо Space Mono**         | у шрифтов макета нет кириллицы; в самом макете русские заголовки рисовались системным шрифтом (ADR-025)                                                                                                                                           |
| **Тёмные оттенки ошибки, предупреждения и фона выбранного чипа**         | оттенки макета не проходят AA по контрасту (DESIGN_BRIEF §12)                                                                                                                                                                                     |
| **Приглушённые уровни и акцентный текст темнее макета**                  | `#94A3B8` давал 2.6:1 на белом, `#0891B2` — 3.7:1; сплошной аудит нашёл 535 таких мест. `--muted` и `--faint` сдвинуты оба, чтобы ступень между уровнями осталась; акценту заведён отдельный текстовый токен, границам прежний оставлен (ADR-158) |
| **Служебные подписи не мельче 12px**                                     | в макете 10–11.5px; складывалось с контрастом 2.6:1 — на телефоне при ярком свете не читалось вовсе. Проверяется `shared/styles/contrast.test.ts` (ADR-158)                                                                                       |
| **Градиент в H1 сужен по темам**                                         | крайняя точка `#22D3EE` на белом даёт 1.9:1                                                                                                                                                                                                       |
| **Логотип 1a «Поток»**                                                   | знак в шапке макета отменён заказчиком (ADR-022)                                                                                                                                                                                                  |
| **Вся мобильная раскладка**                                              | медиа-запросов в макете нет вообще — раскладка ниже 1200px наша (DESIGN_BRIEF §6)                                                                                                                                                                 |
| **Цифры-факты приходят из настроек**                                     | «1200 установок», «3 года гарантии», «1 день» — данные компании: в коде их нет, владелец вводит в админке (ADR-052). Не заполнено — полосы нет                                                                                                    |
| **Нет погодного чипа, частицы урезаны**                                  | внешний запрос и анимация на критическом пути LCP                                                                                                                                                                                                 |
| **Нет фотослотов**                                                       | реальных фотографий пока нет                                                                                                                                                                                                                      |
| **Анимация появления секций**                                            | в реализации на CSS блок навсегда оставался прозрачным на узком экране                                                                                                                                                                            |
| **Первый экран уходит под шапку**                                        | шапка прозрачная и sticky, её фоном оставался основной цвет — над ней была видна ступенька. Внутрь hero шапку перенести нельзя: `position: sticky` ограничен родителем, а `overflow: hidden` у секции отключает его совсем                        |
| **Кольцо фокуса двухслойное, у обрезающих контейнеров врезанное**        | прежний токен был заливкой с прозрачностью 12% и на тёмном фоне не читался; `overflow: hidden` у аккордеона и области прокрутки обрезает внешнее кольцо                                                                                           |
| **Секция формы заявки в контейнере 720px**                               | без контейнера карточка расползалась во всю ширину экрана                                                                                                                                                                                         |
| **Блок «Экономия инвертора» сверке не подлежит**                         | заказчик планирует его переработку — трогать не нужно                                                                                                                                                                                             |
| **Поля первого экрана 32/38 вместо 64/76**                               | с макетными первый экран вырастает до 855px, и полоса доверия не помещается в окно ноутбука (800px), а она обязана быть видна без прокрутки (ADR-047)                                                                                             |
| **Границы контролов витрины на `--line-ui`, а не `--line-strong`**       | линия и есть граница контрола: `--line-strong` даёт на белом 1,48:1 при норме WCAG 1.4.11 в 3:1 (ADR-181). Задело кнопку `bordered`, все поля, чип и зону загрузки — сверка кита с эталоном панели, issue #327                                    |
| **Поле в ошибке залито тинтом, а не только обведено красным**            | одной рамки мало: её не читает ни озвучка, ни глаз при нарушениях цветовосприятия. Приглушённый текст на тинте поднимается на ступень — там он даёт 4,46:1 при норме 4,5 (issue #325)                                                             |
| **Наведение у кнопки, поля и чипа — только под `@media (hover: hover)`** | на сенсорном экране состояние залипает после нажатия: подсвеченной остаётся цель, с которой палец уже ушёл (issue #324)                                                                                                                           |
| **Нажатие кнопки — `transform: scale(.97)`, а не изменение отступов**    | отступы двигают соседей по ряду, и на нажатие отзывалась вся строка действий (эталон HeroUI, issue #324)                                                                                                                                          |

Всё остальное должно совпадать с таблицей ниже — **в обеих темах**.

---

## Спецификация секций

🔴 **Отступ секции в таблице ниже указан неполно**: разбор терял третье значение. В макете секции на основном фоне записаны как `88px 24px 0` — снизу отступа нет. Правило целиком — ADR-037.

`padding` — самое частое значение отступов внутри секции; `H1/H2` — размер главного заголовка; `радиусы` и `размеры шрифта` — самые частые значения в секции.

| Секция макета              | Наш блок                               | Фон секции                                        | padding   | H1/H2 | Радиусы           | Размеры шрифта             |
| -------------------------- | -------------------------------------- | ------------------------------------------------- | --------- | ----- | ----------------- | -------------------------- |
| HEADER                     | widgets/header                         | `(основной)`                                      | 14px 16px | —     | 11px, 18px        | 14px, 17px, 10.5px, 14.5px |
| HERO + ПОДБОР              | widgets/hero                           | `var(--hero-grad)`                                | 15px 28px | 49px  | 12px, 100px, 13px | 13.5px, 12.5px, 28px, 15px |
| TRUST STRIP                | widgets/trust (TrustStrip)             | `var(--panel)`                                    | 22px 24px | —     | —                 | 14.5px                     |
| УСЛУГИ                     | widgets/trust (Services)               | `(основной)`                                      | 88px 24px | 38px  | 20px, 14px        | 21px, 15px, 14.5px, 13px   |
| КАТАЛОГ + СРАВНЕНИЕ        | widgets/catalog                        | `var(--bg-soft)`                                  | 88px 24px | 38px  | 10px, 18px, 100px | 13px, 15px, 22px, 38px     |
| ЭКОНОМИЯ (помощь в выборе) | widgets/installation (SavingsBlock)    | `(основной)`                                      | 88px 24px | 38px  | 100px, 22px       | 17px, 14px, 13px, 15.5px   |
| МОНТАЖ: ЭТАПЫ              | widgets/installation (StepsTimeline)   | `(основной)`                                      | 88px 24px | 38px  | 14px, 26px, 8px   | 13px, 38px, 17px, 20px     |
| ЦЕНЫ НА МОНТАЖ             | widgets/pricing                        | `(основной)`                                      | 16px 24px | 38px  | 11px, 20px, 22px  | 13px, 15px, 38px, 16.5px   |
| ЧЕСТНО О ЦЕНЕ              | widgets/honesty (HonestPricing)        | `(основной)`                                      | 6px 14px  | 38px  | 22px, 100px, 14px | 14.5px, 20px, 15px, 13px   |
| КАК ОБМАНЫВАЮТ             | widgets/honesty (ScamAccordion)        | `(основной)`                                      | 18px 24px | 38px  | 16px, 100px       | 10.5px, 15px, 14.5px, 13px |
| СЕРВИС / ДИАГНОСТИКА       | widgets/service                        | `(основной)`                                      | 14px 16px | 38px  | 12px, 22px, 24px  | 15px, 10.5px, 15.5px, 13px |
| ПОЧЕМУ МЫ                  | widgets/trust (WhyUs)                  | `(основной)`                                      | 88px 24px | 34px  | 28px              | 19px, 14.5px, 34px         |
| ОТЗЫВЫ + ФОРМА ОТЗЫВА      | widgets/reviews + features/review-form | `(основной)`                                      | 12px 14px | 38px  | 11px, 20px, 22px  | 13px, 15px, 14.5px, 22px   |
| ФОРМА ЗАЯВКИ               | features/lead-form                     | `linear-gradient(180deg,#0F172A 0%,#0B2530 100%)` | 13px 14px | 36px  | 11px, 22px, 10px  | 15px, 13px, 16px, 36px     |
| БАЗА ЗНАНИЙ                | —                                      | `var(--bg-soft)`                                  | 88px 24px | 38px  | 18px, 100px       | 13px, 38px, 16.5px, 15px   |
| FAQ                        | —                                      | `(основной)`                                      | 88px 24px | 38px  | 16px              | 13px, 38px, 16.5px, 15px   |
| КОНТАКТЫ + КАРТА           | —                                      | `(основной)`                                      | 88px 24px | 34px  | 12px, 20px, 9px   | 13px, 16px, 34px           |
| FOOTER                     | widgets/footer                         | `var(--panel)`                                    | 48px 24px | —     | 11px, 100px       | 14px, 15px, 13.5px, 14.5px |

---

## Как сверять

1. Открой свой блок в Storybook на **1200px** — это ширина, для которой макет и рисовался. Сверь по таблице: фон секции, вертикальный ритм, размер заголовка, радиусы, размеры шрифта.
2. Переключи **тёмную тему** и сверь снова. Тёмные значения токенов уже описаны в `tokens.css` — расхождения обычно не в значениях, а в том, какой токен взят для поверхности.
3. Проверь **320 / 375 / 768** — здесь макета нет, сверять не с чем; смотри, что ничего не разъехалось и страница не скроллится по горизонтали.
4. Открой соответствующую секцию прототипа в браузере рядом, если сомневаешься: `design/Кондиционеры Тула.dc.html` — обычный HTML-файл.

Расхождение, которое ты считаешь осознанным улучшением, **не исправляй молча**: опиши его в отчёте, и оно либо попадёт в таблицу отступлений, либо будет откачено.

## Отступления, добавленные при сведении вёрстки на все ширины (2026-08-21)

| №   | Место                                | Макет                      | У нас                                            | Почему                                                                                                                                                                                     |
| --- | ------------------------------------ | -------------------------- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 12  | Полоса доверия                       | ряд из четырёх утверждений | ряд с автопрокруткой ниже 1100px                 | В макете нет мобильной версии. Перенос на вторую строку ломает высоту первого экрана (ADR-047), поэтому ряд едет лентой; на широком экране он статичен, как в макете                       |
| 13  | Ряд симптомов диагностики            | шесть кнопок в ряд         | ряд с горизонтальной прокруткой                  | То же основание: перенос уводил выбранный симптом от разбора под ним                                                                                                                       |
| 14  | Витрина каталога и тизер Базы знаний | сетка колонками            | ряд с переносом и центрированным последним рядом | Число моделей и статей задаёт владелец: при остатке от деления жёсткая сетка оставляет карточку одну у левого края                                                                         |
| 15  | Калькулятор монтажа                  | поля в ряд, смета снизу    | две колонки: параметры и смета                   | При появлении строки в смете блок прыгал по высоте                                                                                                                                         |
| 16  | Границы секций с собственным фоном   | `--line-soft`              | `--line`                                         | Разница `--bg` и `--bg-soft` в тёмной теме меньше процента: без видимой границы отступ полосы читается как разрыв ритма                                                                    |
| 17  | Блок «Напомним про чистку»           | поля без подписей          | подписи над полями и чекбокс согласия            | Поле без подписи недоступно с клавиатуры и для экранного диктора: placeholder исчезает при вводе. Чекбокс согласия обязателен по 152-ФЗ (инвариант 12) — в макете его нет вовсе            |
| 18  | Заголовки h1–h6                      | `line-height` не задан     | 1.2                                              | В макете заголовки наследуют браузерное `normal` (около 1.2), а у нас на `body` стоит 1.55 для основного текста — многострочный заголовок разъезжался почти на полтора интервала (ADR-073) |
| 19  | Блок «Напомним про чистку»           | поля столбиком             | телефон и срок в ряд от 520px                    | С подписями и чекбоксом согласия столбик вытягивал панель на треть выше макетной: 386px против 280 (ADR-076)                                                                               |
| 20  | Карточка отзыва в ленте              | состояния наведения нет    | рамка, тень и подъём на 2px                      | Карточка открывает отзыв целиком по нажатию, но выглядела неподвижной: по ней не было видно, что она кликабельна. Движение снимается при `prefers-reduced-motion`                          |
| 21  | Панель управления                    | в макете отсутствует       | собственная раскладка на общих токенах           | Макет описывает сайт; панель — рабочий инструмент владельца, и общими у них остаются только цвета, радиусы и шрифты (ADR-082, ADR-086)                                                     |

## Отступления нижней панели вкладок (2026-08-30)

Макет панели — `design/admin/Phone.body.html`, доска «Панель целиком · 390»
(ADR-185). Расхождения ниже приняты при сборке нижней панели (issue #322).

| №   | Место            | Макет                           | У нас                                                    | Почему                                                                                                                                                                                                                  |
| --- | ---------------- | ------------------------------- | -------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 22  | Значок вкладки   | 17px                            | 22px                                                     | Число из issue #322. Подпись во вкладке — 10.5px, и на 320 она ужимается до предела: значок остаётся единственным, что отличает вкладку с расстояния вытянутой руки                                                     |
| 23  | Открытая вкладка | только цвет подписи и значка    | цвет плюс заливка `--accent-bg` со скруглением `--r-nav` | «Активный — цветом и заливкой» из issue #322. Заливка та же, что у открытого пункта колонки: панель отмечает текущий раздел одинаково на всех трёх ширинах, а цвет в одиночку не читается при нарушении цветовосприятия |
| 24  | Ячейка вкладки   | во всю высоту полосы, без полей | отбита от краёв полосы на 4px                            | В эти четыре пикселя ложится обводка фокуса (2px со сдвигом 2px): без отбивки её обрезал край окна, и первая вкладка при обходе с клавиатуры теряла половину кольца                                                     |

## Отступления денежных блоков (2026-08-31)

Приняты при сборке вехи «Резина · Фаза 5» (issue #264–#268). Артборд —
«Цены и калькулятор» (`design/mobile-home/preview-Money.html`).

| №   | Место                     | Макет и issue                                | У нас                                                | Почему                                                                                                                                                                                                                           |
| --- | ------------------------- | -------------------------------------------- | ---------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 25  | Кегль цены в карточке     | `clamp(19px, 1.05rem + 0.3vw, 20px)`         | `clamp(19px, 0.114vw + 18.636px, 20px)`              | Формула из issue упирается в потолок только на 1067px, то есть от 320 до 1067 кегль стоит. Концы взяты те же (19 → 20), скорость постоянная, потолок на 1200 — как у всей шкалы (DESIGN_BRIEF §6)                                |
| 26  | Кегль суммы итога         | `clamp(24px, 1.3rem + 0.4vw, 26px)`          | `clamp(24px, 0.227vw + 23.273px, 26px)`              | То же основание: формула из issue достигает 26px только на 1300px, за пределом, где контейнер уже не растёт                                                                                                                      |
| 27  | Высота ячейки сетки суток | `clamp(24px, 1.1rem + 0.4vw, 28px)`          | `clamp(26px, 0.227vw + 25.273px, 28px)`              | Формула из issue вырождена: `1.1rem + 0.4vw` не превышает 24px ни на одной ширине, то есть ячейка всегда 24, а не заявленные в том же issue 26. Взяты концы 26 → 28                                                              |
| 28  | Просвет рядов сетки суток | 5px                                          | `calc(var(--tap) - var(--hours-cell))` — 18px на 320 | 5px и тап-зона 44px несовместимы: прозрачные цели соседних рядов перекрываются на 13px, и нажатие по нижнему краю ячейки включает час из ряда ниже (ADR-214)                                                                     |
| 29  | Заливка ночной зоны       | выключенный час `--line`, ночь `--line-soft` | выключенный `--stripe-a`, ночь `--hours-night`       | В светлой теме `--line-soft` **светлее** `--line`, и ночная зона оказалась бы светлее обычного часа — подпись «тёмные ячейки» под сеткой врала бы. Пара оставлена прежней: ночь темнее в светлой теме и темнее фона в тёмной     |
| 30  | Сегмент переключателя     | 34px внутри пилюли 40px                      | до 900px — тап-зона 44px внутри пилюли 50px          | 34px — размер для мыши; на телефоне это ниже минимума 44×44 из §6. Высота приходит переменной контейнера, как у панели (ADR-187), поэтому у кита остаётся один набор классов                                                     |
| 31  | Приглушение кнопки        | 55%                                          | 50%                                                  | Значение отключённой кнопки кита (эталон HeroUI). Второе число рядом с первым означало бы два разных «отключено» в одном интерфейсе                                                                                              |
| 32  | Карточка калькулятора     | `overflow: hidden` при радиусе 22            | радиус 22 без `overflow: hidden`                     | `overflow: hidden` делает карточку прокручиваемым контейнером, и `position: sticky` внутри неё перестаёт работать вовсе — липкий итог из issue #265 был бы невозможен. Свой нижний радиус полоса итога рисует сама               |
| 33  | Высота секций на телефоне | цены ≤ 1400px, экономия ≤ 850px              | цены 1742px, экономия 1395px (замер на 375)          | Бюджеты противоречат требованиям вёрстки из тех же issue: поля калькулятора в столбец и резерв сметы на пять строк делают блок выше, а 420px секции экономии — заголовок и обязательная оговорка про приблизительность (ADR-215) |

## Отступления Базы знаний, вопросов и контактов (2026-09-03)

Приняты при сборке вехи «Резина · Фаза 8» (issue #279–#284, #291).

| №   | Место                          | Макет и issue                         | У нас                                        | Почему                                                                                                                                  |
| --- | ------------------------------ | ------------------------------------- | -------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| 42  | Длина строки статьи и политики | 620–680px                             | `68ch` — замер даёт 622px                    | Пиксели снимались при другом кегле: первая же правка шкалы сделала бы строку длиннее нормы, а знаки держат норму сами (DESIGN_BRIEF §4) |
| 43  | Боковое поле статьи и политики | `clamp(12px, 4vw, 16px)` / `…24px`    | `var(--gutter)` — 16→24                      | Гутер — общий токен фаз 1–7; собственное поле у двух страниц разошлось бы с остальным сайтом. Разница на 320 — 3px                      |
| 44  | Ярлык рубрики                  | 11px                                  | 12px (`Badge size="sm"`)                     | ADR-158 поднял все плашки до 12px по контрасту                                                                                          |
| 45  | Знак у строки вопроса          | шеврон (issue #330)                   | «+» / «−»                                    | Довод про шеврон относился к панели (крестик удаления в списке разделов), а не к публичному FAQ; макет фазы 8 даёт «+»                  |
| 46  | Полоса обложки в карточке      | 96px слева на 600–1199 по ширине окна | от 330px собственной ширины карточки         | На 900 колонок три, карточка 277px: полоса съедала 96 и оставляла заголовку 149px — восемь знаков в строке (ADR-262)                    |
| 47  | Карта в контактах              | штрихованный блок 360px               | блока нет, остаётся карточка «Как нас найти» | Рисунок карты, которой нет, вводит в заблуждение; на телефоне он занимал треть экрана (ADR-265)                                         |

## Отступления отзывов и форм (2026-09-03)

Приняты при сборке вехи «Резина · Фаза 7» (issue #274–#278).

| №   | Место                            | Макет и issue                  | У нас                                  | Почему                                                                                                                                                              |
| --- | -------------------------------- | ------------------------------ | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 34  | Поля секции заявки               | `48px clamp(12px, 4vw, 16px)`  | `var(--section-y) var(--gutter)`       | Общая текучая шкала фаз 1–6; на 375 расхождение 2px, а два набора отступов на одной странице расходятся при первой же правке шкалы                                  |
| 35  | Граница поля на тёмной подложке  | `#2c3d59`                      | `var(--line-ui)`                       | ADR-181: граница контрола обязана давать 3:1 (WCAG 1.4.11), замер даёт 5,4:1. Хекс в CSS-модуле запрещён отдельно                                                   |
| 36  | Имя автора в карточке отзыва     | 14px 700                       | `var(--fs-dense)` 700 — 14,5px         | Токен плотного кегля. Своё число рядом с токеном означало бы два разных «плотно» в одном блоке                                                                      |
| 37  | Дата в карточке отзыва           | `--faint`                      | `--muted`                              | 12px на карточке обязаны добирать AA; `--faint` на этом кегле его не даёт                                                                                           |
| 38  | Форма отзыва                     | две колонки с 900px            | одна колонка на любой ширине           | Окно формы не шире 520px (ADR-254): две колонки давали поля по 230px, куда не помещается ни поле на четыре строки, ни пятая звезда оценки                           |
| 39  | Текстовое поле отзыва            | «минимум 4 строки»             | 5 строк                                | Помещается в окно 686px без прокрутки до кнопки — то есть человек видит и поле, и кнопку разом                                                                      |
| 40  | Действия под отзывами на 320–599 | «два в ряд по половине ширины» | столбиком ниже 344px ширины контейнера | На 320 подпись «Оставить отзыв» резалась многоточием при кнопке 139px (замер). Ряд остаётся рядом там, где обеим кнопкам хватает 150px                              |
| 41  | Высота секции заявки на 375      | ≤ 1000px                       | 1179px (замер)                         | Было 1845. Дальше сокращается только содержание вводной части и состав полей, а они заданы этим же issue: случай под ADR-215, число вынесено в отчёт, а не спрятано |

## Отступления панели от холста «Панель ТулаКлимат» (2026-09-04)

🔴 **Порядок работы задан [ADR-307](DECISIONS.md).** Макет
([холст](https://claude.ai/code/artifact/6283f576-0c4a-4581-99e2-a0da228858e2),
исходники `design/admin/`) решает окончательно состав и порядок блоков, набор и
названия вкладок, композицию и поведение между тремя ширинами. Всё остальное —
геометрия, состояния, полнота действий — проверяется скиллом `ui-ux-pro-max`,
`/code-review`, инвариантами и замерами: **там, где макет расходится с
проверкой, прав не макет.**

Отсюда правило записи: расхождение с макетом либо устранено, либо стоит в
таблице ниже с причиной и номером issue. Третьего состояния — «похоже, и
ладно» — нет. Дефект самого макета (кнопка не в стиле кита, раздел без полного
набора действий над строкой) чинится на ходу и попадает сюда же.

| №    | Место                                                        | Макет и issue                                                                           | У нас                                                                                                                    | Почему                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---- | ------------------------------------------------------------ | --------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| П-1  | Заказы, вкладка «Активные»: галочки строк и колонка действий | `Orders.png` рисует и то и другое, `OrdersTabs.png` — ни того ни другого (#595, #596)   | Галочки и действия строки есть                                                                                           | Два артборда одной вкладки противоречат друг другу. Принят `OrdersTabs` как более поздний, но полнота действий над строкой — требование ADR-307 §4, а не рисунка: раздел без удаления и без группового действия владельцу не нужен                                                                                                                                                                      |
| П-2  | Заказы, действия строки                                      | Три круглых: просмотр, правка, удаление (#595)                                          | Открыть, позвонить, удалить                                                                                              | Глаз и карандаш вели бы в одно и то же место: карточка заказа сегодня и есть форма правки, её разделение — отдельная задача (#598). «Позвонить» — рабочее действие из списка, снятое действие это потеря функции, а не приведение к макету                                                                                                                                                              |
| П-3  | Заказы, значок удаления                                      | Корзина (#595)                                                                          | Крестик `close` из кита                                                                                                  | Корзины в реестре значков кита нет, а `shared/ui/Icon` — общий файл: заводить её в задаче раздела значит менять кит мимо ревью кита. Удаление отличается краснотой и подписью, а не только формой значка                                                                                                                                                                                                |
| П-4  | Заказы, колонка «Действия» на 768                            | Уезжает в карточку заказа (`Orders.png`, подпись артборда)                              | Остаётся на всех ширинах                                                                                                 | Иначе удаление строки недостижимо ровно в одном диапазоне ширин — полнота действий по ADR-307 §4 важнее экономии 126px, тем более что таблица и так прокручивается вбок                                                                                                                                                                                                                                 |
| П-5  | Заказы, вкладка «История»: колонки «Маржа» и «Отзыв»         | Обе есть, плюс маржа в итоге периода (#597)                                             | Ни одной                                                                                                                 | Маржу нечем считать до закупочной цены позиции склада (#628, ADR-310), а разность «сумма минус выплата» маржой не является. У отзыва нет связи с нарядом в схеме вовсе: `Review` не ссылается ни на заказ, ни на клиента. Выдуманное число здесь дороже пустого места                                                                                                                                   |
| П-6  | Заказы, вкладка «Новые»: чип «из заявки № 41»                | Номер обращения в чипе (#597)                                                           | Чип «из заявки» без номера                                                                                               | У `Lead` нет поля номера — показывать нечего. Сам факт «пришло с сайта, а не завели руками» отвечает на вопрос вкладки и без цифры                                                                                                                                                                                                                                                                      |
| П-7  | Заказы, действие «Назначить» на вкладке «Новые»              | Кнопка в строке; куда она ведёт, макет не говорит (#597)                                | Ссылка в карточку наряда                                                                                                 | Монтажника выбирают, видя занятость бригад (ADR-115). Отдельное окно назначения выбирало бы исполнителя вслепую — ровно тем способом, которым переносят выезды                                                                                                                                                                                                                                          |
| П-8  | Заказы, ссылка «Сбросить фильтр»                             | В макете её нет                                                                         | Убрана                                                                                                                   | Условия снимаются крестиками на плашках, как в макете; вторая дорога к тому же действию появлялась от одного переключения вкладки и путала                                                                                                                                                                                                                                                              |
| П-9  | Заказы, шапка раздела: поиск и колокол значками              | Есть на всех артбордах                                                                  | Нет                                                                                                                      | Это оболочка панели, а не раздел: она правится своей задачей (ADR-309), и колокол упирается в отсутствие понятия «прочитано» в схеме                                                                                                                                                                                                                                                                    |
| 48   | Удаление позиции склада                                      | круг «Удалить» в ряду действий, как в каталоге (issue #576)                             | удаления нет вовсе; вместо него «Сдать в архив» / «Вернуть из архива»                                                    | 🔴 **Это решение, а не пробел (ADR-134).** Удаление позиции унесло бы журнал движений — то, ради чего склад и заводится. Архив убирает позицию из работы, ничего не теряя. Не «чинить» обратно: вернуть удаление — значит стереть историю остатков                                                                                                                                                      |
| 49   | Тап-зона действия строки до 900px                            | `.actbtn` 40×40 на телефонном и планшетном кадрах (`_base.css`)                         | 44×44 — `--h-sm` поднимается до `--tap` внутри `[data-ui="panel"]`                                                       | Дефект макета: ADR-183 держит пол цели 44×44 ниже 900px, и 40 его не добирает. Число одно на всю панель и живёт в токене, а не у кнопки                                                                                                                                                                                                                                                                 |
| 50   | Колонка меню строки на складе                                | обычная колонка 44px за «Порогом»                                                       | та же колонка, но залипающая справа; итог сдвинут на её ширину                                                           | У остатков шесть колонок зон и прокрутка внутри контейнера (issue #42, #352). Незалипающее меню пришлось бы доставать прокруткой через все зоны — ровно та беда, из-за которой залип итог. Обе колонки стоят рядом: у итога `right` в ширину меню, а не `0`                                                                                                                                             |
| 51   | Значок меню строки                                           | три точки в ряд (`Stock.body.html`)                                                     | то же — заведён глиф `dots`                                                                                              | Компонент кита рисовал `burger`: три полосы означают навигацию всего экрана, и тот же глиф в строке читается как «меню раздела». Приведено к макету, а не наоборот                                                                                                                                                                                                                                      |
| 52   | «Смотреть на сайте» у скрытых                                | круг активен во всех строках                                                            | отключён у скрытой модели и у черновика, причина — в подписи                                                             | Страницы скрытой модели и черновика на сайте нет: ссылка вела бы в 404 (ADR-109). Действие не исчезает из ряда — исчезнувшая кнопка заставляет гадать, куда она делась                                                                                                                                                                                                                                  |
| П-1  | Действия строки: монтажники                                  | Три круглые кнопки — глаз, карандаш, корзина (`Team.png`, #602)                         | Две круглые кнопки в `TableActions`: «Открыть карточку» и «Удалить монтажника»                                           | «Посмотреть» и «изменить» в панели ведут в одно и то же место — карточку человека, — и вторая кнопка отличалась бы от первой только значком. Удаление закрыто, пока за монтажником есть наряды, и причина висит подсказкой на отключённой кнопке. Значки пока локальные (`features/staff-manager/icons.tsx`): словарь кита закрыт (ADR-194), и три имени в нём — правка общего файла ради одной колонки |
| П-1а | Действия строки: заявки и клиенты                            | Три круглые кнопки со значками (`Leads.png`, `Clients.png`, #601, #602)                 | Меню строки (`RowMenu`) с действиями, названными словами                                                                 | Здесь действий больше двух и они разнородны — позвонить, открыть, удалить: ряд из трёх безымянных значков в очереди на восемь строк читается хуже, чем меню, называющее каждое действие словом. Меню не требует наведения, которого нет ни у пальца, ни у клавиатуры. ADR-307 §2                                                                                                                        |
| П-2  | Список клиентов и список монтажников на 390                  | Не нарисован вовсе: в колонке 390 стоят карточки клиента и монтажника (#604)            | Строки таблицы разворачиваются карточками (`Table variant="cards"`, порог 600px): подпись колонки слева, значение справа | Пробел макета, а не решение (ADR-307 §4). Семь и восемь колонок на 390 дают либо боковую прокрутку документа (нарушение инварианта раскладки), либо нечитаемый кегль. Приём уже принят в панели и в ките — им же собран список заказов                                                                                                                                                                  |
| П-3  | Список монтажников на 768                                    | Четыре колонки: Монтажник · Загрузка · Заработал · Доступ (`Team.png`)                  | Те же восемь колонок, что на 1440, с прокруткой внутри таблицы                                                           | Прятать колонку по ширине — значит держать два состава колонок и два набора `data-label`; вместо этого таблица прокручивается внутри своего контейнера, а документ вбок не уезжает. Порог карточек (600px) ниже 768 намеренно: на планшете таблицу читают таблицей                                                                                                                                      |
| П-4  | Очередь заявок, две колонки                                  | Очередь и карточка рядом уже на 768 (`Leads.png`)                                       | Две колонки от 1200px, ниже — одно из двух по адресу                                                                     | Очередь стала таблицей из пяти колонок (#601): в колонке 260px, которая была у неё на 900–1199, она читается только боковой прокруткой. Ниже 1200 раздел ведёт себя как на 768 в макете — сначала список, карточка занимает экран                                                                                                                                                                       |
| П-5  | Карточка обращения: «Позвонить»                              | Кнопка вживлена внутрь поля телефона, по его краю, без своих границ (`Leads.png`, #601) | Телефон — ссылка `tel:`, на узком экране получающая вид кнопки                                                           | Дефект макета: это либо поле, либо кнопка. Поле ввода вокруг телефона обещает правку, а данные клиента в заявке не редактируются (ADR-013)                                                                                                                                                                                                                                                              |
| П-6  | Карточка обращения: «Закрыть»                                | Третья кнопка в столбике действий (`Leads.png`)                                         | Состояние в поле «Статус»; отказ спрашивает причину окном                                                                | Отмена — состояние, а не кнопка рядом с «Создать заказ» (ADR-310, #630): у неё обязательна причина из справочника, и кнопка без разбора причины давала бы отказ сервера вместо действия                                                                                                                                                                                                                 |
| П-7  | Полоса номеров страниц                                       | Есть во всех списках панели (`Clients.png`, `Leads.png`, #602)                          | Включена в заявках и клиентах (`Pager numbers`); в остальных разделах остаются шаги и подпись положения                  | Разделы приходят к макету разными вехами и разными руками: включённая разом, полоса сдвинула бы геометрию шести списков сразу, в том числе тех, что правятся параллельно. Умолчание пропа переключается одним движением, когда к макету придёт последний раздел                                                                                                                                         |
| П-8  | Плитка «Средняя загрузка · 36 ч · из 40»                     | Норма 40 ч при рабочем окне 09–19 (`Team.png`, #629)                                    | Норма считается как окно × пять рабочих дней: 09–19 даёт 50 ч                                                            | Дефект макета: числа в нём не сходятся между собой. Норма — данные, а не константа (ADR-310); зашить 40 значило бы выдумать факт о компании (инвариант 8)                                                                                                                                                                                                                                               |
| П-10 | Предупреждения в строке команды                              | В макете строка ровная, у нас до правки стоял абзац (`Team.png`, #602)                  | Короткий ярлык («Оформление не заведено», «Самозанятый без ИНН»), объяснение — подсказкой на ярлыке                      | Абзац в ячейке растил строку до ~200px и ломал ритм таблицы, а таблицу читают колонками. Смысл не потерян: ярлык называет состояние сам, `Tooltip` открывается и наведением, и фокусом (WCAG 1.4.13), а полный текст остаётся тем же                                                                                                                                                                    |
| П-11 | Колонка «Загрузка недели»                                    | Полоса и число часов рядом (`Team.png`, #629)                                           | То же плюс чип «+4 ч» у переработки                                                                                      | Переработка не должна опознаваться одной краской (DESIGN_BRIEF §14). Прозой она больше не пишется — это и был абзац из П-10; норма («из 50 ч») ушла в имя и подсказку числа: она одна на всех, и восемь одинаковых «из 50» в колонке — шум                                                                                                                                                              |
| П-9  | Строка отпуска в таблице команды                             | Серая строка «Не работает» с пилюлей «В отпуске» (`Team.png`)                           | Не реализована                                                                                                           | Понятия отпуска в системе нет вовсе: ни поля, ни периода, ни того, кто его ставит. Нарисовать пилюлю можно только выдумав данные (инвариант 8). Заводится отдельным issue, если владелец подтвердит, что отпуск нужен учётом, а не закрытием доступа                                                                                                                                                    |
| П-12 | Плашка «Обращение № 39 ждёт больше суток»                    | Над рядом фильтров, сразу под подстрокой (`Leads.png`, #601)                            | Внутри блока очереди, под фильтрами                                                                                      | Плашка условная — появляется, когда обращение ждёт дольше суток. Над фильтрами она двигала бы всё, что под ней, а верх списка обязан стоять на месте между данными, пустотой и ошибкой: сквозной замер требует нулевого разброса. Внутри блока её место занимает то, что блок показывает вместо неё, и верх не двигается                                                                                |
| 48   | Удаление позиции склада                                      | круг «Удалить» в ряду действий, как в каталоге (issue #576)                             | удаления нет вовсе; вместо него «Сдать в архив» / «Вернуть из архива»                                                    | 🔴 **Это решение, а не пробел (ADR-134).** Удаление позиции унесло бы журнал движений — то, ради чего склад и заводится. Архив убирает позицию из работы, ничего не теряя. Не «чинить» обратно: вернуть удаление — значит стереть историю остатков                                                                                                                                                      |
| 49   | Тап-зона действия строки до 900px                            | `.actbtn` 40×40 на телефонном и планшетном кадрах (`_base.css`)                         | 44×44 — `--h-sm` поднимается до `--tap` внутри `[data-ui="panel"]`                                                       | Дефект макета: ADR-183 держит пол цели 44×44 ниже 900px, и 40 его не добирает. Число одно на всю панель и живёт в токене, а не у кнопки                                                                                                                                                                                                                                                                 |
| 50   | Колонка меню строки на складе                                | обычная колонка 44px за «Порогом»                                                       | та же колонка, но залипающая справа; итог сдвинут на её ширину                                                           | У остатков шесть колонок зон и прокрутка внутри контейнера (issue #42, #352). Незалипающее меню пришлось бы доставать прокруткой через все зоны — ровно та беда, из-за которой залип итог. Обе колонки стоят рядом: у итога `right` в ширину меню, а не `0`                                                                                                                                             |
| 51   | Значок меню строки                                           | три точки в ряд (`Stock.body.html`)                                                     | то же — заведён глиф `dots`                                                                                              | Компонент кита рисовал `burger`: три полосы означают навигацию всего экрана, и тот же глиф в строке читается как «меню раздела». Приведено к макету, а не наоборот                                                                                                                                                                                                                                      |
| 52   | «Смотреть на сайте» у скрытых                                | круг активен во всех строках                                                            | отключён у скрытой модели и у черновика, причина — в подписи                                                             | Страницы скрытой модели и черновика на сайте нет: ссылка вела бы в 404 (ADR-109). Действие не исчезает из ряда — исчезнувшая кнопка заставляет гадать, куда она делась                                                                                                                                                                                                                                  |
