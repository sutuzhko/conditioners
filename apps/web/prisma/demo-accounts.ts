/**
 * Учётные записи, которые заводит демонстрационный сид.
 *
 * 🔴 Вынесены из `seed-demo.ts` отдельным модулем не ради порядка, а потому
 * что их читает второй потребитель — сквозные сценарии. Сам сид импортировать
 * нельзя: у него `main()` вызывается на верхнем уровне, и импорт ради одной
 * строки стёр бы стенду данные.
 *
 * Пока роль ставится только сидом (заведение через панель и API создаёт
 * монтажника — план «Роли», Фаза 6), это единственный менеджер, который на
 * стенде есть. Выписать его логин в сценарии второй раз значило бы завести
 * пару, которая разъедется молча: сценарий не упал бы, а просто перестал
 * входить.
 */

/**
 * Пароль один на все демо-учётки и заведомо несекретный: стенд открыт только
 * внутри дев-контура. Хеш считается argon2id тем же вызовом, что и в админке.
 */
export const DEMO_PASSWORD = 'demo-parol-2026';

/** Логин демонстрационного менеджера (ADR-344). */
export const DEMO_MANAGER_LOGIN = 'lebedeva';
