-- Перенос: до появления флага витрина главной показывала всё, что в продаже.
-- Без этой строки добавление колонки со значением по умолчанию `false` молча
-- опустошило бы главную на первом же деплое.
UPDATE "Product" SET "featured" = true WHERE "visible" = true;
