/**
 * Тексты журнала событий.
 *
 * Подписи действий и сущностей живут не здесь, а в `entities/activity/model`:
 * там же, где перечень действий, — иначе новое действие можно было бы завести,
 * забыв его назвать.
 */
import type { AdminRole } from '@/entities/staff/model';

/**
 * Названия ролей для отбора.
 *
 * 🔴 Своя копия, а не общий словарь из `widgets/admin-shell`: слой `features`
 * не имеет права импортировать из `widgets` (правило зависимостей). Ровно та
 * же копия и по той же причине стоит в `features/profile-form`. Полнота
 * держится типом — новая роль не даст собраться, пока её не назвали.
 */
const ROLE_TITLES: Readonly<Record<AdminRole, string>> = {
  owner: 'Владелец',
  admin: 'Администратор',
  manager: 'Менеджер',
  installer: 'Монтажник',
};

export const activityLogContent = {
  title: 'Журнал событий',
  lead: 'Кто и когда что менял в панели',

  colWhen: 'Когда',
  colWho: 'Кто',
  colAction: 'Действие',
  colEntity: 'Над чем',

  /** Автора не было по природе: событие сайта, расписание, кнопка в Telegram. */
  noAuthor: 'Система',

  /**
   * Автор был, а учётной записи больше нет.
   *
   * 🔴 Не «Система» и не прочерк. «Система» соврала бы — действие совершил
   * человек; прочерк читается как «данные потеряли», хотя потеряно только имя.
   * Не «Удалённый сотрудник»: в панели, где заводят монтажников по всей
   * области, «удалённый» прочитается как «работающий не в офисе».
   */
  authorGone: 'Учётная запись удалена',

  tableLabel: 'События панели',
  pagerLabel: 'Страницы журнала',

  emptyTitle: 'Событий пока нет',
  emptyText:
    'Журнал заполняется сам: как только кто-нибудь что-нибудь изменит в панели, здесь появится строка.',

  /* 🔴 Пустой журнал и пустой результат отбора — разные новости с
     противоположными шагами (issue #335). В первом случае ждать, во втором —
     снять условия, и текст обязан говорить, какой из двух случаев на экране. */
  notFoundTitle: 'Ничего не найдено',
  notFoundText:
    'За этот период и по этим условиям событий нет. Снимите часть отбора или расширьте период.',

  loadFailed: 'Не удалось загрузить журнал',
  filtersLoadFailed: 'Не удалось загрузить отбор',

  // ---------- Отбор ----------

  filterLabel: 'Отбор журнала',
  filterActor: 'Кто',
  filterActorAll: 'Любой сотрудник',
  filterRole: 'Роль',
  filterRoleAll: 'Любая роль',
  filterSection: 'Раздел',
  filterSectionAll: 'Все разделы',
  filterEntity: 'Над чем',
  filterEntityAll: 'Любая сущность',
  filterFrom: 'С какого дня',
  filterTo: 'По какой день',
  filterSubmit: 'Показать',
  filterReset: 'Сбросить',

  /**
   * Пометка человека у записи.
   *
   * 🔴 Не «Комментарий»: комментарий читается как часть события, а пометка —
   * то, что дописал человек рядом с ним. Разница видна ровно тогда, когда
   * кто-то решит, что журнал можно дополнять.
   */
  noteLabel: 'Пометка',
  noteAt: (when: string): string => `Пометка от ${when}`,

  roleTitle: (role: AdminRole): string => ROLE_TITLES[role],
} as const;
