import type { ButtonLinkHref } from '@/shared/ui';

/**
 * Фикстуры историй и тестов. Телефон выдуманный и живёт только здесь: на сайт
 * он приходит из настроек компании, в коде его нет (инвариант 8).
 */
export const policyHrefFixture: ButtonLinkHref = '/privacy';

export const phoneFixture = '+7 (900) 000-00-00';
