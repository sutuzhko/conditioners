import { PRIVACY_PATH, SITE_ROUTES, type SiteRoute } from './routes';

/**
 * Тексты страницы 404 и её навигация (docs/SEO.md §5).
 *
 * Контент отделён от вёрстки, как и везде: строки правятся чаще разметки.
 * Фактов о компании здесь нет — 404 ничего не обещает и никуда не звонит.
 */
export const NOT_FOUND_CONTENT = {
  code: '404',
  title: 'Такой страницы нет',
  lead:
    'Возможно, адрес набран с опечаткой или страницу перенесли. ' +
    'Ниже — разделы, ради которых сюда обычно и заходят.',
  navTitle: 'Куда перейти',
  homeLink: 'Вернуться на главную',
} as const;

/**
 * Разделы в помощь заблудившемуся. Главная в список не входит — на неё ведёт
 * отдельная кнопка над ним; политика в этом перечне только мешает.
 */
export const NOT_FOUND_ROUTES: readonly SiteRoute[] = SITE_ROUTES.filter(
  (route) => route.path !== '/' && route.path !== PRIVACY_PATH,
);
