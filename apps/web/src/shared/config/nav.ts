import type { NavItem } from '@/widgets/header';

/**
 * Навигация сайта. Здесь перечислено только то, что на сайте действительно
 * есть: ссылка на несуществующий маршрут или якорь — это мёртвая ссылка,
 * которую видят и посетитель, и робот. Пункты добавляются по мере готовности.
 *
 * Разделы кластера ведут на свои страницы, а не на якоря главной: у каждого
 * интента своя посадочная (docs/SEO.md §1), и навигация — главный источник
 * перелинковки на них.
 */
export const SITE_NAV: readonly NavItem[] = [
  { label: 'Каталог', href: '/catalog' },
  { label: 'Цены', href: '/prices' },
  { label: 'Монтаж', href: '/installation' },
  { label: 'Сервис', href: '/service' },
  { label: 'Отзывы', href: '/reviews' },
  { label: 'База знаний', href: '/knowledge' },
  { label: 'Контакты', href: '/contacts' },
];

/**
 * Якорь секции заявки. Он есть на каждой странице кластера и на главной;
 * страницы без формы (например, политика) кнопку на него не ставят.
 */
export const LEAD_ANCHOR = '#zayavka';

/** Политика обработки персональных данных: страница есть, ссылка строкой. */
export const POLICY_HREF = '/privacy';
