/**
 * Подписи действий строки списка панели — общие на все разделы.
 *
 * 🔴 Лежат здесь, а не в словаре раздела (issue #745). Клиенты и монтажники —
 * два списка людей в одной панели, и требовать от владельца двух разных
 * привычек нельзя: набор и порядок пунктов у них один. Две копии одних и тех
 * же слов разъедутся на первой правке формулировки — так уже разъезжались
 * подписи вкладок, пока их не свели в `admin-tabs`.
 *
 * Здесь только то, что действительно общее. Подпись, называющая саму запись
 * («Удалить клиента», «Удалить монтажника»), остаётся у раздела: она про его
 * предметную область, а не про приём.
 */
export const rowActionTexts = {
  /** Переход в карточку записи — то же, что нажатие по строке. */
  open: 'Открыть карточку',
  call: 'Позвонить',
  /**
   * Второй уровень меню: что именно скопировать (ADR-351).
   *
   * 🔴 Не «Скопировать телефон» одним пунктом: полей в строке несколько, и
   * пункт на каждое растит меню тем быстрее, чем полнее строка. Выбор поля
   * вторым уровнем оставляет первый уровень коротким.
   */
  copy: 'Скопировать',

  /* Поля второго уровня. Названы так же, как колонки таблиц, — человек ищет
     в меню то слово, которое видит в шапке. */
  fieldName: 'Имя',
  fieldPhone: 'Телефон',
  fieldAddress: 'Адрес',
} as const;
