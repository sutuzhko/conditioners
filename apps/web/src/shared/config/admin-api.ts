/**
 * Общие тексты ошибок админских запросов — умолчания для `adminRequest`
 * (shared/lib/api.ts). Фича может заменить сетевую и серверную формулировки
 * своими, но текст про истёкшую сессию один на всю панель: до общего хелпера
 * шесть фич из девяти вовсе не отличали 401 от прочих отказов, и владелец
 * читал «сервер не принял изменения» вместо «войдите заново» (аудит, BUGS).
 */
export const ADMIN_API_TEXTS = {
  network: 'Сеть недоступна. Проверьте соединение и попробуйте ещё раз',
  server: 'Сервер не принял изменения. Попробуйте ещё раз',
  session: 'Сессия истекла — войдите в панель заново',
} as const;
