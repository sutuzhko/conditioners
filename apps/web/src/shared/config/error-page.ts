/**
 * Тексты экрана падения рендера (`app/error.tsx`).
 *
 * Живут в конфиге, а не в БД: экран появляется ровно тогда, когда база
 * недоступна, — читать оттуда нечего. По той же причине здесь нет телефона:
 * он хранится в настройках компании, а выдумывать номер код не вправе
 * (инвариант 8).
 */
export const ERROR_PAGE_CONTENT = {
  code: 'Ошибка',
  title: 'Сайт временно не отвечает',
  lead: 'Что-то сломалось на нашей стороне, и мы это уже видим. Обновите страницу — обычно этого достаточно. Не помогло — загляните через несколько минут.',
  retry: 'Попробовать ещё раз',
  homeLink: 'На главную',

  /* 🔴 Выходы вместо тупика (issue #291). Экран падения рендера — публичная
     страница, и уходить с неё человек должен по ссылке, а не кнопкой «назад».
     Ссылки ведут на разделы сайта: их адреса — карта маршрутов, а не данные
     компании, и читать для них базу не нужно. */
  navTitle: 'Разделы сайта',
  brandLabel: 'На главную страницу',
  policyLabel: 'Политика обработки персональных данных',
} as const;
