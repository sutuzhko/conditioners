/**
 * Заглушка сидов. Она специально заметна глазом: правдоподобный
 * «+7 (4872) 00-00-00» уехал бы в прод незаметно и прожил там год
 * (PROJECT §3, «Сиды»).
 *
 * Живёт в shared, а не в entities: это техническая метка «поле не заполнено»,
 * общая для сидов, проверки готовности и сборщиков разметки, — фактом о
 * компании она не является, а слой shared не может тянуть её из entities
 * (правило зависимостей, ADR-096).
 */
export const SETTING_PLACEHOLDER = 'ЗАПОЛНИТЕ В АДМИНКЕ';
