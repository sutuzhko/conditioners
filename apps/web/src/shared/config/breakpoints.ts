/**
 * Пороги адаптива для кода, который спрашивает ширину у браузера, а не у CSS
 * (DESIGN_BRIEF §6). Набор тот же — 600, 900, 1200; здесь он записан в виде
 * медиа-запроса для `matchMedia`.
 *
 * 🔴 Константа общая, потому что порог делит поведение двух компонентов
 * ровно пополам: ниже 600 показывается липкая панель действий, с 600 —
 * плавающая кнопка «наверх», и место у нижнего края у них одно. Разойдись
 * значения на единицу — и на 600 не будет ни того, ни другого, а на 599
 * окажутся оба.
 *
 * Запрос записан старой формой `min-width`, а не диапазонной `width >= `:
 * `matchMedia` с незнакомым синтаксисом молча отвечает «не совпало», и на
 * движке постарше кнопка «наверх» исчезла бы совсем — без единой ошибки.
 */
export const WIDE_SCREEN_QUERY = '(min-width: 600px)';
