/**
 * Заголовки, которыми middleware разговаривает с панелью.
 *
 * Константа лежит в `shared`, а не в `middleware.ts`: её читает и edge-код
 * ворот, и серверный layout панели, и тянуть ради одной строки весь модуль
 * middleware в серверный компонент не за чем.
 */

/** Адрес запроса. Layout панели решает по нему, пускать ли роль (ADR-095). */
export const ADMIN_PATHNAME_HEADER = 'x-admin-pathname';
