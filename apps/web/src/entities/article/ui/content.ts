/**
 * Служебные подписи статьи.
 *
 * 🔴 Ни одной строки самой статьи: заголовок, рубрику, анонс и дату правит
 * владелец в админке, и в код они не попадают (docs/CLAUDE.md, «Контент»).
 * Здесь — только то, что произносит читалка, и `alt` обложки.
 *
 * Живут в сущности, а не в виджете: одни и те же подписи нужны карточке в
 * тизере главной, карточке листинга и странице статьи, а импорт вбок между
 * виджетами запрещён правилом слоёв.
 */
export const articleLabels = {
  /** Время чтения: «6 мин чтения». Слово «мин» не склоняется — падежей не нужно. */
  minutesLabel: (minutes: number): string => `${minutes} мин чтения`,
  /** Подпись даты для скринридера: голосом «14 июня 2026» без пояснения непонятно. */
  dateLabel: 'Опубликовано:',
  categoryLabel: 'Рубрика:',
  /**
   * `alt` обложки с географией, как требует docs/DESIGN_BRIEF.md §8.
   *
   * ⚠️ Город здесь зашит и это нарушение инварианта 8 — строка перенесена
   * из виджетов как есть, чтобы перевёрстка не меняла заодно текст `alt`.
   * Чинится отдельной задачей: город обязан приходить из настроек.
   */
  coverAlt: (title: string): string => `${title} — статья о кондиционерах в Туле`,
  /**
   * Подпись плашки, которая занимает место обложки, когда файла нет (ADR-127).
   * Обычно на ней стоит рубрика статьи; эта строка — запасной вариант для
   * статьи, у которой не заполнена и рубрика.
   */
  coverFallbackLabel: 'База знаний',
} as const;
