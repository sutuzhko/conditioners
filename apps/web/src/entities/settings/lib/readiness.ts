/**
 * Проверка готовности настроек живёт в `server/repo/settings.ts`
 * (`checkReadiness`, docs/API.md §5): у неё есть список обязательных полей
 * по группам, и одна такая проверка на проект — правильное число.
 *
 * Сама заглушка переехала в shared (ADR-096) — она нужна и сборщикам
 * разметки, которым entities недоступен; реэкспорт сохраняет прежним
 * привычный адрес для доменного кода.
 */
export { SETTING_PLACEHOLDER } from '@/shared/config/placeholder';
