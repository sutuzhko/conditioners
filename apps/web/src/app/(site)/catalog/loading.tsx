import { CatalogListSkeleton } from '@/widgets/catalog';

/**
 * Каталог на время перехода.
 *
 * Страница читает `searchParams` и рендерится динамически: нажатие на фильтр,
 * порядок или номер страницы — полная навигация, во время которой Next держит
 * старую страницу. Без этого файла у перехода нет ни одного признака жизни
 * (CLAUDE.md, «Каждый асинхронный блок данных имеет скелетон»).
 *
 * Вводная часть здесь не повторяется: её текст статичен, но дублировать
 * разметку страницы ради двух абзацев дороже, чем показать место, где вот-вот
 * появятся карточки.
 */
export default function CatalogLoading() {
  return <CatalogListSkeleton />;
}
