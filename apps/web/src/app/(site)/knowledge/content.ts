import { ARTICLES_PATH, SITE_ROUTES } from '@/shared/seo';

/**
 * Тексты маршрутов Базы знаний: метаданные листинга и подписи перехода из
 * статьи в коммерческие разделы.
 *
 * 🔴 Ни одного факта о компании (инвариант 8): город приходит аргументом из
 * настроек — `place` это готовый хвост «в Туле» или пустая строка, если город
 * не заполнен. Заголовки и тексты самих статей сюда не попадают ни при каких
 * условиях: они живут в базе и правятся в админке.
 */
export const bazaZnaniyContent = {
  /**
   * Подпись раздела в хлебных крошках. Берётся из карты адресов, чтобы след,
   * карта сайта и страница 404 называли раздел одинаково.
   */
  sectionTitle: SITE_ROUTES.find((route) => route.path === ARTICLES_PATH)?.title ?? 'База знаний',

  /** Шаблон заголовка листинга — docs/SEO.md §3. */
  metaTitle: 'База знаний о кондиционерах — выбор, установка, уход',
  metaDescription: (place: string): string =>
    `Как подобрать кондиционер по площади, из чего складывается цена монтажа ` +
    `и как ухаживать за техникой. Пишут мастера, которые ставят кондиционеры` +
    `${place}.`,

  /**
   * Перелинковка из статьи вглубь сайта (docs/SEO.md §5, «Контент»): анкор
   * называет страницу, а не «подробнее». Подписи лежат рядом с адресами,
   * а не внутри виджета: карта URL принадлежит странице.
   */
  catalogLink: 'Каталог кондиционеров с ценами',
  installationLink: 'Установка кондиционеров под ключ',
} as const;
