/**
 * Тексты и адрес страницы сравнения (ADR-121).
 *
 * 🔴 Ни одного факта о компании и ни одной характеристики товара
 * (инвариант 8): названия моделей, цены и характеристики приходят из базы.
 */

export const comparePageContent = {
  /** Подпись раздела в хлебных крошках. */
  sectionTitle: 'Сравнение',

  kicker: '— Сравнение —',
  title: 'Сравнение кондиционеров',
  /* Лид не повторяет приглашение из пустого состояния: один и тот же абзац
     дважды на одном экране — это дубль, а не подсказка. */
  lead: 'Характеристики и цена под ключ у отмеченных моделей — рядом, в одной таблице.',

  /**
   * Заголовок вкладки. Страница закрыта от индекса, но заголовок ей всё равно
   * нужен: его видит человек в списке вкладок и в пересланной ссылке.
   */
  metaTitle: 'Сравнение кондиционеров',
  metaDescription: (place: string): string =>
    `Сравнение отмеченных моделей кондиционеров${place}: характеристики и цена ` +
    `под ключ вместе с монтажом в одной таблице.`,
} as const;
