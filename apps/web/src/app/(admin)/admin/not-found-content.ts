/**
 * Подписи страниц «не найдено» внутри панели — issue #631.
 *
 * Тексты стоят отдельно от разметки по общему правилу проекта, и по той же
 * причине, что у страницы отказа: это разговор с человеком, который уже
 * вошёл, и формулировку правят чаще, чем вёрстку.
 *
 * 🔴 Случая два, и они разные. Несуществующий **адрес** — опечатка или
 * устаревшая ссылка: раздела нет и не было. Несуществующая **запись** —
 * адрес верный, а клиента или наряда по нему больше нет: чаще всего его
 * удалили из соседней вкладки. Один текст на оба случая отправлял бы
 * проверять адрес там, где проверять нечего.
 *
 * 🔴 Ни одного факта о компании (инвариант 8): страница ничего не обещает и
 * зовёт только внутрь панели.
 */
export const PANEL_NOT_FOUND_CONTENT = {
  code: '404',
  address: {
    title: 'Страница не найдена',
    /* 🔴 Про «колонку слева» здесь не говорят: на 390 колонки нет вовсе —
       разделы живут в нижней полосе. Подсказка, верная на одной ширине из
       трёх, хуже её отсутствия. */
    lead: 'Такого раздела в панели нет. Проверьте адрес — возможно, в нём опечатка или ссылка устарела. Открытые вам разделы перечислены в меню панели.',
  },
  record: {
    title: 'Запись не найдена',
    lead: 'Записи по этому адресу больше нет — скорее всего, её удалили. Вернитесь к списку: там видно, что осталось.',
  },
  /**
   * Выход разный у ролей, и это не украшение: монтажник, отправленный на
   * сводку, получил бы 403 вместо выхода из тупика (ADR-092, ADR-095).
   */
  owner: { label: 'К сводке', href: '/admin' },
  installer: { label: 'К своим работам', href: '/admin/crm' },
} as const;

/** Какой из двух случаев показывает страница. */
export type PanelNotFoundKind = 'address' | 'record';
