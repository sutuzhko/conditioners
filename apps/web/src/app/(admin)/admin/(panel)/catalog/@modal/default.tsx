/**
 * Слот окна вне перехвата пуст.
 *
 * Без этого файла Next не знает, что рисовать в `@modal` при заходе на любой
 * другой адрес раздела, и отдаёт 404 на ровном месте: слот обязан уметь
 * ничего не показывать.
 */
export default function CatalogModalDefault() {
  return null;
}
