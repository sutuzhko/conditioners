import { AdminSummarySkeleton } from '@/widgets/admin-shell';

/**
 * Состояние перехода на сводку — и запасной скелетон для разделов без своего.
 *
 * 🔴 Переходы в админке — клиентская навигация: документ не перезагружается,
 * но данные каждого раздела читаются на сервере по запросу (`force-dynamic`).
 * Без этого файла Next держит на экране прежнюю страницу, пока идёт ответ, и
 * нажатие выглядит непроизошедшим — отсюда ощущение «зависшей загрузки».
 *
 * Файл стоит на группе `(panel)`, то есть отвечает за `/admin` — сводку, и
 * повторяет её раскладку (issue #334). Прежний нейтральный скелетон
 * «заголовок, две строки, полотно» обещал не ту раскладку: плитки приезжали
 * на 110px, а полотно стояло на 113px.
 */
export default function PanelLoading() {
  return <AdminSummarySkeleton />;
}
