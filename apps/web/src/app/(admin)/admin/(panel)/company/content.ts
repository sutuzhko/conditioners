/** Подписи страницы данных компании. */
export const companyPageContent = {
  title: 'Компания',
  /* 🔴 Процент готовности в подзаголовке не называется: он назван у полосы, и
     два одинаковых числа на экране расходятся при первой же правке одного из
     них (ADR-307, дефект макета — см. PIXEL_SPEC §«Панель»). */
  lead: 'Контакты, адрес, реквизиты и метаданные сайта — а рядом рабочее окно календаря, единственная группа, которой на сайте нет. Правки появляются на сайте сразу после сохранения.',
  back: '← Настройки',

  /* Плашка стоит над формой, а не под ней: она объясняет, зачем на экране
     полоса готовности и почему часть групп подсвечена. */
  unfilledTitle: (count: number): string =>
    `${count} ${plural(count, 'группа', 'группы', 'групп')} компании ${plural(count, 'не заполнена', 'не заполнены', 'не заполнено')}`,
  unfilledText:
    'Пока группа пуста, на сайте вместо этих данных стоят заглушки: телефон, адрес и реквизиты берутся только отсюда. Публиковать сайт в таком виде нельзя.',
} as const;

/** Склонение после числа: 1 группа, 2 группы, 5 групп. */
function plural(count: number, one: string, few: string, many: string): string {
  const mod100 = count % 100;
  if (mod100 >= 11 && mod100 <= 14) return many;

  const mod10 = count % 10;
  if (mod10 === 1) return one;
  if (mod10 >= 2 && mod10 <= 4) return few;
  return many;
}
