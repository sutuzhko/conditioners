/**
 * Подписи страницы-указателя «Настройки».
 *
 * Названия и пояснения самих разделов сюда не переезжают — они живут в списке
 * разделов оболочки, и второй их список разошёлся бы с колонкой.
 */
export const settingsPageContent = {
  /* 🔴 Своё пояснение, а не из списка разделов оболочки: там оно называет три
     страницы, а указатель показывает ещё и справочник характеристик
     (ADR-283). Число в тексте обязано совпадать с числом карточек, поэтому
     чисел в нём нет вовсе. */
  lead: 'Страницы конфигурации: заполняются однажды и правятся редко. Адреса разделов не меняются — прежние ссылки продолжают работать.',

  /** Справочник характеристик: про товар, а не про организацию (ADR-094). */
  specsTitle: 'Справочник характеристик',
  specsHint: 'Группы и типовые названия характеристик моделей',

  readyTitle: 'Заполнено',
  /* 🔴 Числа настоящие: считаются по тем же группам, что показывает
     «Компания». Выдуманный процент готовности хуже отсутствующего. */
  unfilled: (count: number): string =>
    `${count} ${plural(count, 'группа', 'группы', 'групп')} не заполнено`,

  warningTitle: 'Данные компании заполнены не полностью',
  warningText:
    'Пока группа пуста, на сайте вместо этих данных стоят заглушки: телефон, адрес и реквизиты берутся только из настроек. Публиковать сайт в таком виде нельзя.',
  warningAction: 'Заполнить данные компании',
} as const;

/** Склонение после числа: 1 группа, 2 группы, 5 групп. */
function plural(count: number, one: string, few: string, many: string): string {
  const mod100 = count % 100;
  if (mod100 >= 11 && mod100 <= 14) return many;

  const mod10 = count % 10;
  if (mod10 === 1) return one;
  if (mod10 >= 2 && mod10 <= 4) return few;
  return many;
}
