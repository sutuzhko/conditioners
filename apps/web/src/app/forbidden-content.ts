/**
 * Подписи страницы отказа — issue #353.
 *
 * Тексты стоят отдельно от разметки по общему правилу проекта: страница
 * отказа — это разговор с человеком, который уже вошёл, и формулировку
 * правят чаще, чем вёрстку.
 *
 * 🔴 Ни одного факта о компании (инвариант 8): отказ ничего не обещает и
 * никуда, кроме собственного рабочего экрана, не зовёт.
 */
export const FORBIDDEN_CONTENT = {
  code: '403',
  title: 'Раздел закрыт',
  /**
   * 🔴 Текст безличный, а не «доступен только владельцу» (ADR-344). Ролей
   * четыре, и прежняя формулировка врала бы менеджеру: раздел, закрытый ему,
   * бывает открыт администратору, и «только владельцу» тут неправда. Что
   * именно открыто человеку, он видит в колонке своей панели; отказу
   * достаточно сказать, что этот раздел не его, и вывести из тупика.
   */
  lead: 'Этот раздел панели вашей учётной записи не открыт. Кнопка ниже вернёт вас на ваш рабочий экран.',

  /**
   * Выход — рабочий экран роли, и ключ есть у каждой (ADR-344).
   *
   * 🔴 Пока ролей было две, выход был один — календарь монтажника: больше
   * его увидеть было некому. С менеджером тот же адрес стал вторым отказом
   * подряд: календарь ему не открыт, и человек ходил бы по кругу 403 → 403.
   *
   * Адрес совпадает с первым разделом колонки этой роли, и совпадение
   * проверяется тестом рядом: разойдясь, выход снова стал бы тупиком.
   */
  owner: { label: 'К сводке', href: '/admin' },
  admin: { label: 'К заявкам', href: '/admin/leads' },
  manager: { label: 'К заявкам', href: '/admin/leads' },
  installer: { label: 'К своим работам', href: '/admin/crm' },

  /**
   * Сессия истекла между проверкой доступа и отрисовкой отказа. Звать такого
   * человека в раздел панели бессмысленно — его развернут на вход; отправляем
   * туда сразу, но ссылкой, а не разворотом: код ответа остаётся 403.
   */
  guest: { label: 'Войти в панель', href: '/admin/login' },
} as const;
