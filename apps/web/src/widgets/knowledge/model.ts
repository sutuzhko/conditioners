/**
 * Что тизеру нужно от статьи.
 *
 * 🔴 Тип не описывается здесь заново, а приходит из сущности: тот же набор
 * полей рисует карточка листинга, и пока копий `Pick` было две, они
 * расходились молча. Реэкспорт нужен, чтобы у блока остался свой публичный
 * API — страница импортирует тип отсюда, а не лезет в сущность через голову.
 *
 * 🔴 Блок в базу не ходит: отбор опубликованных статей и их порядок — дело
 * страницы (docs/ORCHESTRATION.md, «Блок не ходит в базу»).
 */
export type { ArticleTeaser } from '@/entities/article/model';
