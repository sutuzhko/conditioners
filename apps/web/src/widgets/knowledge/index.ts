/**
 * Публичный API тизера Базы знаний. Страница импортирует отсюда и передаёт
 * статьи пропсами — сам блок в базу не ходит (docs/ORCHESTRATION.md).
 */
export { KnowledgeTeaser } from './KnowledgeTeaser';
export type { KnowledgeTeaserProps } from './KnowledgeTeaser';
export type { ArticleTeaser } from './model';
