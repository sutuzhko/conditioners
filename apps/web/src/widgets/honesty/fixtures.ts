/**
 * Фикстуры блока «Честность о цене». Документируют, что блок ждёт от
 * страницы: из базы он не читает ничего (docs/ORCHESTRATION.md, волна 3).
 */

/**
 * Минимальная цена монтажа, ₽ — так выглядит значение, пришедшее из прайса.
 * Число здесь и есть смысл фикстуры: в самом блоке цены нет (инвариант 8).
 */
export const installFrom = 6000;

/** Другой прайс: цена изменилась в админке — заголовок обязан поехать следом. */
export const higherInstallFrom = 7500;

/** Якорь статьи в Базе знаний — полный разбор схем обмана. */
export const scamArticleHref = '#kak-obmanyvayut-pri-ustanovke';
