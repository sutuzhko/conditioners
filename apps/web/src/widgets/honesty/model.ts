/**
 * Контракт блока «Честность о цене».
 *
 * 🔴 Блок в базу не ходит: разборы и списки — статический текст из
 * `content.ts`, цифры приходят пропсами (docs/ORCHESTRATION.md, «Блок не
 * ходит в базу»).
 */

/** Пункт сравнения двух смет: строка списка «что входит» или «на чём экономят». */
export type HonestyPoint = {
  /** Ключ строки: нумерация списка не годится, порядок пунктов правят. */
  readonly id: string;
  readonly text: string;
};
