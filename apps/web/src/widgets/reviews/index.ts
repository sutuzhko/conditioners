/**
 * Публичный API блока «Отзывы». Страница импортирует отсюда и передаёт список
 * одобренных отзывов пропсами — сам блок в базу не ходит (docs/ORCHESTRATION.md).
 */
export { Reviews } from './Reviews';
export type { ReviewsProps } from './Reviews';
export type { ReviewCardData } from './model';
