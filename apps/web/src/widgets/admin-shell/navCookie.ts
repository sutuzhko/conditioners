/**
 * Имя cookie с состоянием боковой колонки.
 *
 * 🔴 Отдельным модулем, без `'use client'`: константу читает серверный layout,
 * а импорт из клиентского модуля Next подменяет ссылкой на клиентский код —
 * на сервере вместо имени приходило пустое значение, и колонка всегда
 * открывалась развёрнутой, что бы владелец ни выбрал.
 */
export const NAV_COOKIE = 'admin-nav';

/** Год — столько живёт настройка вида. К сессии она отношения не имеет. */
export const NAV_COOKIE_MAX_AGE = 60 * 60 * 24 * 365;
