/**
 * Публичный API блока «Заявка». Страница импортирует отсюда и передаёт телефон,
 * адрес политики и обещанное время ответа пропсами — сам блок в базу не ходит
 * (docs/ORCHESTRATION.md).
 */
export { LeadSection } from './LeadSection';
export type { LeadSectionProps } from './LeadSection';
