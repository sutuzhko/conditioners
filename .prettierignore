**/.next
node_modules
pnpm-lock.yaml
**/storybook-static
coverage
apps/web/public/fonts
design

# Скиллы установлены из чужих репозиториев и лежат в git как есть (ADR-149).
# Переформатировать их нельзя: содержимое перестанет совпадать с источником и
# с хешем в skills-lock.json, а обновление скилла превратится в конфликт
# форматирования вместо диффа по смыслу.
.agents
.claude/skills

# Рабочие копии репозитория из скилла `github-flow` — тот же код в другой
# ветке. Форматировать его отсюда нельзя: правка уедет в чужую ветку мимо
# её автора (issue #417).
.claude/worktrees
